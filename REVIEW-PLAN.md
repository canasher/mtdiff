# mtdiff 代码 Review 修复计划（2026-08-28 制定，2026-08-31 状态复核）

> 来源：全项目严格 Review（build / vet / `go test -race` / gofmt 全部通过）。
> 原结论（08-28）：暂缓发布——3 个 P1 静默假阴性 bug（会把不一致的表报成"一致"，exit 0）。
>
> **状态汇总（2026-08-31 逐项复核）**：
>
> | 级别 | 项数 | 状态 |
> |---|---|---|
> | P1 | 3 | ✅ 全部已修复，单测回归在位 + e2e 实测通过 |
> | P2 | 6 | ✅ 全部已修复（P2-4/5/7/9 带单测回归；P2-6/8 代码核实） |
> | P3 | 11 | ✅ 全部已修复（08-31 实施，逐项证据见下；另抓出 1 个潜伏正确性 bug） |
>
> 原"暂缓发布"阻断项（P1）已解除。2026-08-31 验证记录：build / vet / gofmt /
> `go test -race -count=1 ./...` 全绿；e2e 全套 **45 项断言**通过、零跳过（含 json-report
> 与 parallel-determinism 两节；此前因本机无 jq 跳过，装 jq 后补跑通过；42→45 为 P3-#15
> 新增 `t_chunkc` 复合键场景 +2 断言）。期间还修掉 `e2e/run_e2e.sh` 一处潜伏 bug：json 节
> 的 mtdiff 调用未防 `set -e`，有差异时 exit 1 会无声终止整个套件（此前因整节跳过从未暴露）。
> P3 实施中新 e2e 场景抓出 **`keyRow` 复合键 ORDER BY 方向 bug**（见 P3-#15 附注），属潜伏
> 正确性问题，已一并修复并纳入 e2e 回归。
> 注意：本环境 `go` 不在默认 PATH，工具链在 `/home/liukl/sdk/go/bin`；e2e 需 docker（`make e2e`）。

## P1（阻断，发布前必须修复）— ✅ 全部完成

### P1-1 ✅ 已修复（08-31 复核）：整型键切块 off-by-one
- 原位置：`internal/chunk/plan.go`（`intBoundaries`）
- 原 Bug：`step := (hi - lo + n - 1) / n` 是 `ceil((hi-lo)/n)`，覆盖闭区间 `[lo,hi]`（hi-lo+1 个值）应为 `ceil((hi-lo+1)/n)`。当 `(hi-lo) % n == 0` 时 step 小 1，末块 Hi 止于 `hi-1`，最大键值行被两侧同时漏扫 → 两侧对称漏扫 → 假"一致"。
- **现状**：`plan.go:149` 已改 `step := (hi - lo + n) / n`，`intBoundaries` 注释（:138-141）专门记录了该坑。
- **验证**：单测 `TestIntBoundariesDivisible`（`plan_test.go:80-98`：(1,7,3)、(0,99,11)、(1,90001,10) 可整除用例）；e2e `t_chunk` id 1..90001 + `--chunk-size 10000`，`m_chunk_max.sql` 改最大 id 行 → exit 1（`run_e2e.sh:111-117`）。

### P1-2 ✅ 已修复（08-31 复核）：NULL 键值被排除出所有块谓词
- 原位置：`internal/chunk/plan.go`（extremes）、`internal/chunk/predicate.go`、`internal/conn/schema.go`（`SelectKey`）
- 原后果：① 单列可空唯一键：NULL 键行不被任何块扫描但 `COUNT(*)` 计入 → 假"一致"。② 复合键前导列可空且含 NULL：所有块为空 → 两侧行数相同的任意两张表判"一致"。③ 键值全 NULL：报 "empty key range" 硬错误。
- **现状**：
  1. `SelectKey`（`schema.go:229-244`）关联 `COLUMNS.IS_NULLABLE`（`nullableColumns` :249），只选**所有列均 NOT NULL** 的唯一索引；否则回退无键路径（KeySource "none"）。
  2. extremes（`plan.go:57-78`）改用 `ORDER BY … LIMIT 1` 取首/末行（不再 MIN/MAX，NULL 安全）；谓词层对 NULL 边界显式处理（`predicate.go:56-69` 单列 `k IS NULL OR k <= hi` 等；`rowCompare` :112-148 复合键 NULL 分量生成 `k1 IS NULL AND …` 分支）。
  3. 显式 `--key` 命中可空列时打印告警（`comparer.go:311-335` `applyKey` + `keyNullabilityWarns`）。
- **验证**：单测 `TestPredicateNULLBounds`（`plan_test.go:142-184`）；e2e `t_nullkey`（无 PK、可空唯一键）自动降级无键比较，`m_nullkey_change.sql` 改 NULL 键行 → exit 1（`run_e2e.sh:118-123`）。

### P1-3 ✅ 已修复（08-31 复核）：秒级小数规范化碰撞
- 原位置：`internal/normalize/value.go`（`formatMySQLTime` / `formatMySQLDateTime`）
- 原 Bug：把微秒计数当整数 `TrimRight(..., "0")`：1ms/10ms/100ms 全部渲染 `.1`（所有 10 的幂次小数秒两两碰撞）。
- **现状**：先零填充到 6 位再裁尾零（`value.go:173-174` TIME、`:190-191` DATETIME）：`frac := fmt.Sprintf("%06d", f); strings.TrimRight(frac, "0")`。
- **验证**：单测 `TestTimeFormatting`（`normalize_test.go:107-152`：1ms→`.001`、10ms→`.01`、100ms→`.1` 互不相同；DATETIME 10ms/100ms 同）+ `TestFractionalSecondCollisions`；e2e `t_fracsec` 100ms vs 10ms，`m_fracsec_change.sql` → exit 1（`run_e2e.sh:124-127`）。

## P2（建议发布前处理）— ✅ 全部完成

1. **P2-4 ✅** `internal/compare/comparer.go:342` `filterIgnored`：原 = `--ignore-columns` 非空时 dst 独有列被静默丢弃（ignore 为空时同样漂移却硬报错，行为不一致）。**现状**：重建 dstKeep 后，dst 中既不在 keep 也不在 ignore 的列报错（:359-361 `"destination column %s is neither compared nor listed in --ignore-columns"`），两路径行为统一。单测 `TestFilterIgnoredDrift`（`compare_test.go:44`：src 2 列/dst 3 列、ignore 拼错名、被 ignore 的 dst 独有列、全 ignore、缺列）。
2. **P2-5 ✅** `cmd/mtdiff/diff.go:61-85`：原 = `--tolerance 0`/`--drill-limit 0`/`--max-allowed-packet 0` 用 `!= 0` 判断，无法覆盖 YAML 非零值。**现状**：五处（parallel/chunk-size/tolerance/drill-limit/max-allowed-packet）全部 `cmd.Flags().Changed(...)`，diff.go 无残留 `!= 0` 检查。单测 `TestApplyOptionsZeroOverrides`、`TestApplyOptionsIllegalValues`（`diff_test.go:27` 起）。
3. **P2-6 ✅** `internal/conn/conn.go`：原 = `AcquireScan` 每块 checkout 重放 5 条 SET；`sql_mode = CONCAT(@@sql_mode,…)` 非幂等，超 `sql_max_mode_size` 后每块一条 stderr 告警。**现状**：`OpenSide`（:118-127）一次性预热 scan 池（每连接应用一次策略），`AcquireScan`（:197-203）退化为纯 checkout，每块零 SET 往返；`addSQLModeFlags`（:163-185）先读 `@@SESSION.sql_mode`、缺失才补 flag，幂等，mode 不再增长。（无专门单测，代码核实；e2e 万行级扫描未见告警噪音。）
4. **P2-7 ✅** `internal/compare/drilldown.go`：原 = 无键表整表单块 `--drill` 时整表两侧行物化进 map，大表 OOM。**现状**：`drillMaxRows = 100_000`（:49），`bufferRows`（:141-187）超限停止缓冲、继续抽干迭代器、返回 `truncated=true`；`comparer.go:155-158` 一次性 stderr 告警 "…capped at 100000 rows per side; row-level results are a sample (truncated)"。内存有界（~1e5 行/侧）。单测 `TestDrillDownRowCap`。
5. **P2-8 ✅** `.github/workflows/ci.yml:15,29` `go-version: "1.25"` 与 `go.mod:3` `go 1.25.0` 已一致（1.25 ≡ 1.25.0，无需工具链自动下载兜底）。
6. **P2-9 ✅** 测试缺口已补：新增 `internal/compare/compare_test.go`（`TestFilterIgnoredDrift`/`TestDrillDownRowCap`/`TestApplyKey`/`TestFoldDigests`——后者覆盖行数不等分支，原"无 DB 不可测"）与 `cmd/mtdiff/diff_test.go`（`TestApplyOptionsZeroOverrides`/`TestApplyOptionsIllegalValues`）；e2e 已接入 P1 三场景（`run_e2e.sh:111-127`）。全部测试文件：`compare_test.go`、`diff_test.go`、`conn_test.go`（TestClassify/QuoteIdent/BuildDSN/Compatible）、`config_test.go`（ParseShorthand/MaskedDSN/LoadFileEnvExpansion/ResolvePasswordPriority/ValidateAndDefaults）、`plan_test.go`（IntBoundaries/IntBoundariesDivisible/Predicate/PredicateNULLBounds/Literal/ValuesEqual）、`normalize_test.go`（Decimal/TLVLayout/NULLVsEmptyVsZero/FormatFloat/TimeFormatting/FractionalSecondCollisions/FormatBit/NormalizeJSON/StringOptions/RowValueTypes）、`hash/digest_test.go`（顺序依赖、指纹块序无关、SumSq/Xor 缺口、Secure）。

## P3（按需）— ✅ 全部 11 项已修复（08-31 实施完成）

1. **P3-10 ✅** Validate 顺序：`build()`（`flags.go`）与 `diffRunE`（`diff.go`）两处均改为先 `Validate()` 再 `ApplyDefaults()`（defaults 会改写 <=0 值，旧顺序让负值检查成为死代码）。`Validate` 补 DrillLimit 负值检查，全部错误信息带实际值；`--config` 场景报错前缀配置文件路径。单测 `TestValidateAndDefaults`（负 parallel/chunk_size/drill_limit/tolerance 均拒）。
2. **P3-11 ✅** `password_env` 指向未设置的变量现在启动即报 `password_env %q is set but the environment variable is not`（原先 `os.Getenv` 静默返回空 → 无密码连接）；区分未设置（报错）与设置但为空（合法，`os.LookupEnv`）。单测 `TestResolvePasswordEnvMissing`。
3. **P3-12 ✅** 文档化 `${ENV}` 限制：MANUAL.md / README.md 注明"解析前原文替换，值含引号/换行/冒号会破坏 YAML，只建议用于密码这类简单值"。
4. **P3-13 ✅** 两侧扫描并发：`compare` 内 errgroup 各起一 goroutine 跑 `scanSide`（src/dst 各自 chunk 并发不变），墙钟 src+dst → max。`pickScanError` 归因：一侧因另一侧错误被取消（`context.Canceled`）时，报告存活侧的真实错误，前缀 `src scan:`/`dst scan:`。单测 `TestPickScanError`（6 用例含取消归因）。
5. **P3-14 ✅** 行数不等短路：`srcTotal != dstTotal` 且未 `--drill` 时跳过全量行扫描，直接 `DIFFERENT`（`--drill` 仍需逐行明细不短路）。单测 `TestSkipRowScan`（表驱动含 drill=true 分支）。
6. **P3-15 ✅** 复合键前导 INT 算术切分：`integerSingleKey` → `integerLeadKey`（前导列 INT/UINT 即可）；`intBoundaries(lo, hi, n, leadPrefix)`，复合键边界只含前导列单值并带 `LoPrefix/HiPrefix`；谓词渲染为普通列比较（`` `a` > 7501 AND `a` <= 15002 ``）而非字典序展开。单测 `TestIntBoundariesLeadPrefix`（1..30001 可整除形状 + prefix 断言）、`TestPredicateLeadPrefix`（含全宽 prefix 仍走字典序）；e2e `t_chunkc`（复合 PK 30001 行、`--chunk-size 10000` 可整除）改最大前导值行 → exit 1。
   - **附注（计划外发现，潜伏正确性 bug，已修）**：新 e2e 场景首跑报"一致"，排查发现 `keyRow` 渲染 `ORDER BY a, b DESC`——**SQL 方向只作用于末列**，复合键的 max 行被读成 min 行（a 最小 + 其下 b 最大），整表键域塌缩成一行、两侧对称塌缩 → 静默假"一致"。复合键此前无任何 e2e 覆盖，潜伏至今。修复：`keyOrder` 每列重复方向（`a DESC, b DESC`）。单测 `TestKeyOrder`；e2e `t_chunkc` 两断言即回归。
7. **P3-16 ✅** 消除每行拷贝：`Normalize` 改收 `[]any`（database/sql 不能把 NULL Scan 进 `*driver.Value`，只能 `[]any`；元素仍是 driver 值，any→driver.Value 直接可赋值）；`Scan` 复用 `[]any` 扫描缓冲直接送 Normalize，删掉每行 make+copy；drill 的 `next()` 同理。单测全部 `[]any` 化后 `go test -race` 全绿，e2e 全过。
8. **P3-17 ✅** `chunk_size` 下限：`const MinChunkSize = 10`，显式正值低于下限被拒（`chunk_size 5 is below the minimum of 10`），0 仍表示"用默认"。单测 `TestValidateAndDefaults`（5 拒、10 收）。
9. **P3-18 ✅** 发布 commit（`c7316d5`）：README 时区描述改为实际机制（session 级 `SET time_zone` +08:00/-04:00，非系统时区），.cnf 文件保留为可选替代并说明；附"已在 MySQL 8.0 验证"注记。
10. **P3-19 ✅** drill 键渲染防碰撞：`lookupKey` 对 string/[]byte 分量 `strconv.Quote(shorten(t))`、其余走 `renderValue`，`shorten` 超 77 字符截断加 `...`；`("a","b | c")` 与 `("a | b","c")` 不再碰撞，`"42"` 与 int 42 可区分。单测 `TestLookupKeyCollision`（分隔符碰撞、类型碰撞、500 字符截断）。
11. **P3-20 ✅** 计数差专用 RowKind：`RowCountDiff = "COUNT_DIFF"`，`multisetDiff` 用 switch 区分三态（两侧都在但计数不等 → COUNT_DIFF；`s == nil` → MISSING_IN_SRC；`d == nil` → MISSING_IN_DST）。单测 `TestMultisetDiffKinds`。

## 已核实非缺陷（勿重复排查）

- 并发：scanSide channel 逻辑正确（缓冲 len(chunks)、closer goroutine、无泄漏/死锁），`-race` 通过（08-31 复跑）；TableFingerprint 按 ID 排序与并发无关。
- 安全：DSN 用 `url.UserPassword` 编码、全链路打码（e2e 泄漏断言 08-31 实测通过）；标识符反引号转义 + information_schema 参数化，无注入；`--where`/`--key` 是操作者自供 SQL（文档化设计）；两级只读护栏逻辑正确。
- DECIMAL 不走 float64、BIT 跨宽按数值、NULL/空串/0 TLV 独立 tag——均正确且有测试。
- 依赖、跨平台（纯 SQL、无本地文件操作、CI 有 Windows 交叉编译）无问题。
- ~~原测试缺口根因~~（08-31 已解决）：`TestIntBoundaries` 曾全避开可整除情形、e2e `t_large` 跨度 99999 不被 10 整除恰好避开 P1-1、`TestTimeFormatting` 只测 500ms——现已补 `IntBoundariesDivisible`、`FractionalSecondCollisions`、`t_chunk`/`t_nullkey`/`t_fracsec` e2e 场景，同类"全绿仍漏 P1"的盲区已消除。

## 仍未验证的风险（08-31 更新）

- ~~e2e 未实际运行~~ → **已运行**（2026-08-31，`make e2e`，**45 项断言**全过、零跳过；jq 装于 `~/bin/jq` 1.7.1 后 json-report 与 parallel-determinism 两节实测通过）。
- TiDB / PolarDB-X 与 MySQL 5.7 兼容性未验证（e2e 只覆盖 MySQL 8.0 容器）。
- 千万行级性能未实测（P2-6/P2-7 修复已落地并有单测，P3-13/14/16/17 性能改进已落地，但大规模行为仍是代码推导，建议发布后找一张千万行级真实表跑一次基准）。~~已补：10M 行基准实测（09-02 更新节）~~
- ~~TiDB / PolarDB-X 与 MySQL 5.7 兼容性未验证（e2e 只覆盖 MySQL 8.0 容器）~~ → **5.7（5.7.44）与 TiDB（v7.5.1）已按 `make compat-57` / `make compat-tidb` 实测通过（2026-09-03，见下节）；PolarDB-X 仍未实测**。

## 发布状态

**已发布 v0.1.0**（2026-08-31 决策：发布）。P1 三个静默假阴性 + P2 六项全部修复并经单测 + e2e 双重验证后，打 annotated tag `v0.1.0`（指向 `c7316d5`；源码 `Version` 为 `0.1.0-dev`，发布构建需 `-ldflags "-X mtdiff/cmd/mtdiff.Version=v0.1.0"`）。

P3 十一项于同日实施完毕（commits `a762295`/`d1f9ed0`/`ff2441b`），并在此过程中发现并修复了 `keyRow` 复合键 ORDER BY 潜伏 bug（见 P3-#15 附注）。

**已发布 v0.2.0**（2026-09-02 决策）：在 v0.1.0 + P3 之上，新增 **sync 命令**（dry-run 默认零写入 / `--apply` 确认后单写连接只写 dst / 写后自动复验 / 结构同步前置默认开启、`--no-sync-schema` 关闭）+ stderr 进度显示（~10% 一行）。全部经单测（含 `-race`）+ e2e **81 项断言**（MySQL 8.0 docker 双实例，零跳过）验证后打 annotated tag `v0.2.0`（发布构建 `-ldflags "-X mtdiff/cmd/mtdiff.Version=v0.2.0"`；源码 `Version` 保持 `0.1.0-dev` 惯例不变）。

剩余未验证项：TiDB/PolarDB-X 与 MySQL 5.7 兼容（结构同步依赖 information_schema 默认值渲染，5.7 未实测）；1 亿行级为线性外推（10M 行已实测，见 09-02 更新节）。属兼容性/性能风险而非正确性阻断。仓库为纯本地 git（无 remote，按需求方要求不推远程）。

## 更新（2026-09-02）

- **千万行性能基准已实测**（docker 双 MySQL 8.0，聊天形态 1000 万行表，BIGINT 主键；宿主机有其他负载、数字偏保守）：`diff` 10M×2 相同数据 parallel 4 ≈ 2m09s、`--parallel 16` ≈ 48s；sync row-level 补 5M 缺失行（含全量复验）≈ 8m11s（写入段 ~14k 行/s，单写连接）；FULL 1M 重灌+复验 ≈ 1m49s（~11k 行/s）。线性外推 1 亿行：diff ≈ 20min（p4）/ 8min（p16）；row-level sync ≈ 45min 固定开销 + 增量；FULL ≈ 2.5~3h。详见 README"性能"节与 MANUAL"性能"节。
- 同期新增 **sync 命令**（dry-run 默认零写入 / `--apply` 只写 dst / 写后自动复验）+ stderr 进度显示（~10% 一行，不影响 stdout 报告与 `--json`），review 修复后 e2e 扩至 **67 项断言**全过。注意：sync 全部改动尚未 commit（工作区 + untracked `internal/sync/` 等），v0.1.0 tag 之后。
- ~~明确不修（留单独一轮）：dst 键落在 src 键范围外的行首轮不删~~ → **已修（2026-09-02，见下条）**。
- **sync 新增"先同步表结构"前置步骤**（默认开启，`--no-sync-schema` 跳过）：dst 结构漂移（缺列 / 类型或可空或默认值变化 / 多余列 / 缺主键或唯一索引）时，每表生成一条 `ALTER TABLE`（子句序 DROP INDEX → DROP COLUMN → MODIFY COLUMN → ADD COLUMN → ADD INDEX；ADD COLUMN 用 FIRST/AFTER 恢复 src 列序；索引按列序比较、与索引名无关；DATETIME↔TIMESTAMP 互换不产生 DDL，仍交 `--allow-tz-swap`）。流程：dry-run 先显示 DDL（零写入，exit 1）；`--apply` 确认后 TRUNCATE（先清空，保证 NOT NULL 无默认值补列安全）→ 执行 DDL → 重新 introspect/compare → 全量重灌 + 复验。dst 多余列 DROP 掉（与 TRUNCATE 全量重灌语义一致）；`--ignore-columns` 的列同时排除在数据与结构同步之外；src 列默认值为非字面表达式（8.0.13+ `(expr)`）拒发 DDL、该表报错（不伪造值）。实现：`conn.IntrospectStructure`（information_schema，现有 diff 路径的 IntrospectTable 不动）+ `internal/sync/struct.go`（纯函数 DiffStructure/RenderDDL，单测覆盖）；`--where` 与结构漂移组合明确报参数错误。退出码：sync 结构漂移 dry-run 由旧行为 2（FAILED）变为 1（待 DDL）；diff 严格只读、行为不变。e2e 新增 `t_struct` 种子 + `m_struct_drift.sql`（5 个场景 / 14 项断言：dry-run DDL 展示 / 零写入证明 / apply 后 information_schema 列集·列序·类型·PK 内容断言 / 仅结构漂移单独收敛 / 结构相同无 DDL / `--no-sync-schema` 回归 exit 2），断言总数 67→81。注意：本轮改动与上一轮 sync 全部改动**均未 commit**（工作区 + untracked `internal/sync/` 等），v0.1.0 tag 之后。
- **范围外 dst 行删除（上文"明确不修"项，单独一轮完成）**：ROWLEVEL 同步现在显式扫描并删除 dst 中键值**严格**落在 src 键范围 [min, max] 之外的行。实现：`chunk` 新增 `Extremes`（ORDER BY 读首/末键，NULL 安全、带 --where，空表返回 nil）+ 严格比较器 `RenderLessThan`/`RenderGreaterThan`（`strictCompare`：与 `rowCompare` 平级不复用——其包含式上界 NULL 分支会把边界行本身算进去；严格 `<`/`>` 把等值行留给 chunk diff；NULL 界分量逐分支 NULL 安全；`LiteralFunc` 支持逐列渲染器）；`sync` 新增 `keyLits`（family 感知字面量：字符/decimal/time 键按引号字符串而非 hex 比较，避免按字节序错判内外）+ `scanKeyRows`（dst 只读扫描仅取键列）+ `outOfRangeDeletes`（先读 src 极值 → 渲染范围外谓词 → 扫 dst → 按规范化键恒等排序 → 逐键 opDelete、按 --batch-size 分组事务，追加在范围内 chunk 组之后），接在 `RowOps` 末尾。dry-run 示例 SQL / apply 分组事务 / 写后复验零改动自动生效。效果：无 `--where` 首轮直接收敛（FULL 升级保留为计数失配安全网）；有 `--where` 删匹配过滤条件的范围外行，不匹配的保留（文档化残留：过滤表不能 TRUNCATE，复验报 1、无过滤 diff 可见）；键型两侧不一致（结构漂移未对齐）时跳过而非冒险误删。e2e 新增 `t_oor`/`t_oorc`/`t_oors`/`t_oorn` 种子 + 6 个 `m_oor*.sql`（6 组场景 / 34 项断言：int 键范围外增删改 / 复合键含严格边界探针 (1,0)/(10,4) 与边界行 (1,1)/(10,3) 保留 / VARCHAR 键引号字面量路径 / NULL 最小键表（--key 显式、NULL 多重集保留）/ --where 残留（匹配删、不匹配留、无过滤 diff exit 1）/ 无 --where 等行数首轮收敛（dry-run 有 DELETE 无 TRUNCATE）），断言总数 113（80 退出码 + 33 输出内容；此前"81 项"为检查条目口径，实际 ok 行 79）。
- **范围外删除轮独立复查（2026-09-03，对抗性 review + 逐项验证）**：对全部改动逐类别复查（strictCompare NULL 真值表逐分支对照 MySQL 排序、`[]any` 转换类型保真、guard 可达性、RowOps 接线、samples 行为、e2e 假绿、泄漏/注入）。确认并修复 2 个新问题：① `outOfRangeDeletes` 的 nil 界 guard 只查 `minV`——补 `maxV == nil`（`RenderGreaterThan(cols, nil)` 会 panic，已实测确认；正常路径 `Extremes` 双 nil 已被 minV guard 挡住，此属防御加固）；② 键一致性 guard 原来只比键**个数**——`--no-sync-schema` 下 src PK `(a,b)` vs dst PK `(b,a)`（列型兼容可过 pre-pass）会把 src 键值渲染到 dst 错位列上、**删掉范围内行**（丢数据）→ 新增 `keyAgree`（名称+顺序一致才放行，纯内存检查前置到 DB 读取之前）+ `TestKeyAgree` 7 例钉死。其余类别（范围内误删/漏删、真值表、类型保真、接线、采样器、e2e 假绿、资源/注入）逐项确认无问题。遗留 1 项**既有**（非本轮引入）：一侧有键、另一侧无键时 `Chunk.Predicate`/`scanSide` index-out-of-range panic（已实测）——**已于 2026-09-03 修复（见下条）**。
- **既有 keyed/keyless panic 修复（2026-09-03，闭环轮）**：一侧有可用键、另一侧没有（如 dst 丢了主键的结构漂移）时，旧代码用 src 的键规划 chunk 并把同一份 chunk 应用到两侧扫描 → 对无键侧的 schema 渲染非空界值谓词 → index-out-of-range panic（`mtdiff diff` 直接可达；sync 经 `--no-sync-schema` 可达）。修为可比较而非报错：① `compare.compareTable`：两侧可用键不一致时强制两侧 keyless 全表模式（单一无界 chunk + 顺序无关的多重集指纹，报告里附 warn 说明回退）——两侧 `Cols` 均含全部物理列，指纹形状一致，数据相同仍 OK、真实漂移仍 DIFFERENT；② `compare.DrillDown`：仅当**两侧都有键**才按键匹配，键不一致时两侧都按多重集缓冲（键列仍在两侧 `Cols` 中，规范化行仍可比较）——修复旧代码形状不匹配的 row map 会误报"全部缺失"；③ `sync.DecidePlan`：ROWLEVEL 现在要求**两侧**都有可用键（原来只看 src）——任一侧无键 → FULL（TRUNCATE + 重灌）；无键 + `--where` → 参数错误。`TestDecidePlan` 扩为 13 例矩阵（含 keyed src/keyless dst → FULL、+where → ArgErr、双无键 → FULL/ArgErr）。e2e 新增 `t_keyless` 种子 + `m_keyless_drift.sql`（仅删 PK、数据不变）+ `m_keyless_data.sql`（行数保持 50：删 3 / 改 1 / 增 3，使比较真正走多重集扫描而非计数短路）：diff 回退（同数据 exit 0 + warn 断言 / 数据漂移 exit 1）/ sync `--no-sync-schema` dry-run FULL（TRUNCATE 样本）/ +`--where` exit 3 / apply 收敛（删除行恢复、被改行复原、多余行清掉的 3 项内容断言）/ 默认结构同步自愈（ADD PRIMARY KEY DDL 断言 + information_schema PK 恢复断言），共 16 项。断言总数 113→127（89 退出码 + 38 输出内容）。上述各轮已 commit + annotated tag `v0.2.1`（`ad533d7`）。

## 更新（2026-09-03）：跨后端兼容实测轮（MySQL 5.7 + TiDB）

- **背景**：此前所有 e2e 只跑 MySQL 8.0 容器，5.7/TiDB 属"按兼容性设计支持但未实测"。本轮新建 `e2e/compat/`（`make compat-57` / `make compat-tidb`）：种子 SQL 无递归 CTE（5.7 没有）、`TIMESTAMP NULL DEFAULT NULL`（5.7 双裸 TIMESTAMP 报 1067）、同一文件两侧通用；场景集 69 项断言（核心 diff/sync/tables、`--where` 零匹配删除、范围外删除 int/复合/NULL 键、keyless 全量重灌、结构同步 DDL、键漂移回退）。5.7 套件（5.7.44 双容器）首跑即 69/69 全过。
- **TiDB 套件（mysql:8.0 源 15308 + 单节点 `pingcap/tidb:latest` = v7.5.1 目标 15309）暴露并修掉 4 个真实不兼容**：
  1. **无会话级只读**：TiDB 的 `read_only` 仅 GLOBAL 级（1229），`SET SESSION TRANSACTION READ ONLY` 是禁用的空操作（1235，除非 `tidb_enable_noop_functions`）→ 两级护栏全失败，mtdiff 默认拒绝连接（安全护栏，不静默放宽）。新增 `--allow-unenforced-readonly`（各子命令，默认 off）：豁免时逐连接告警继续，读连接仍只发 SELECT；实现 = `config.Options.AllowUnenforcedReadOnly`（YAML `allow_unenforced_readonly`）+ `connFlags` 注册 + `conn.OpenSide`/`applySession` 透传（diff/sync/tables 六个调用点）。同时修正 `applySession` 旧注释的错误说法（"TiDB 直接支持会话 read_only"）。
  2. **无密码服务端的连接串**：TiDB 默认 root 无密码，`root@host` 走交互询问 → 非 TTY 下 exit 3。新增 `user:@host` 显式空密码语法（`config.Endpoint.passwordSet` 非导出字段：YAML 不可设；`ResolvePassword` 中压制询问但让位于 `password_env`），单测 `TestParseShorthand` 新用例 + `TestResolvePasswordExplicitEmpty`。
  3. **结构比较跨后端假漂移**：同一份 DDL 建的表，8.0 报 `int`/`utf8mb4_0900_ai_ci`（其默认），TiDB 报 `int(11)`/`utf8mb4_bin`（其默认）→ `colDiffers` 字符串直比误报 1 条 DDL、把"结构相同"的表拖进 TRUNCATE 全量重灌。修：`normalizeIntType`（整数显示宽度 `int(11)`≡`int`，含 unsigned 形态；decimal/char/enum 不动）+ 默认 collation 等价（`conn.DefaultCollation` 查库级/服务器默认，`Runner` 每 run 一次缓存、查询失败降级严格比较并告警）：两侧 collation 各自等于自己后端的库默认 → 不漂移；显式声明的不同仍出 DDL。单测 `TestNormalizeIntType` 12 例 + `TestDiffStructureCrossBackend` 6 例。
  4. **TiDB 单条 ALTER 禁止同列双操作**（8200 "Unsupported operate same column"，实测 `MODIFY COLUMN a …, ADD PRIMARY KEY (a)` 被拒、拆两条成功）：`RenderDDL` 改为——索引操作引用的列在本批被 MODIFY/ADD 时拆到**第二条** ALTER（列操作先落地）；索引整体随被删列消失时**不再显式 DROP**（两后端都随列自动删，显式 DROP 会在列删后报 1091，已实测）。无冲突时仍单条（主 e2e 8.0 DDL 断言逐字不变）。单测 `TestRenderDDLSplitSameColumn`/`TestRenderDDLSkipImplicitIndexDrop`。
- **结果**：TiDB 套件 69/69 全过（含范围外删除边界探针、NULL 键多重集、结构同步 DDL 应用+复验、键漂移默认同步补回 PK）；5.7 套件复跑 69/69；主 e2e 8.0 复跑 127/127；单测含 `-race` 全绿、`make lint` 通过。本轮改动（compat 套件 + 4 项修复 + 文档）**未 commit**。
- **文档**：README 安全性节（TiDB 例外与 flag）、连接串 `user:@` 语法、flag 表、验证状态（5.7/TiDB 已实测）、MANUAL 对应节 + 新 FAQ（跨后端结构归一 + TiDB DDL 拆分）。

## 更新（2026-09-03）：数据安全优先修复轮（P0-1/2/3、P1-1~5、P1-BIGINT、P2-1~3）

- **背景**：按"src 永远只读 / dst 只在 `sync --apply` 确认后写 / 默认 dry-run 零写入 / 有唯一键优先 ROWLEVEL / 能逐行修复禁止无意义全表重灌"原则对全部写路径做数据安全检查，独立 review（planning / write / false-green 三路）+ 逐项修复 + 复跑三套集成。原则：不重构架构、不删兼容能力，MySQL 8 / 5.7 / TiDB 兼容尽量保持。
- **P0-1（`--where` + 非唯一 `--key` 写入前拒绝）**：显式 `--key` 解析出唯一性（`conn.ExplicitKeyIsUnique` 对索引目录：主键或 NOT NULL UNIQUE 索引才算唯一，可空列上的唯一索引不算，与自动选键同规则；`Schema.KeyIsUnique` 落到两侧）。`DecidePlan` 新增 `srcUnique/dstUnique/key` 参数：`--where` + 任一侧非唯一 → `ModeError + ArgErr`（退出码 3），dry-run 与 `--apply` 都在**任何写连接建立之前**拒绝。无 `--where` 的非唯一键安全（引擎按组整组替换，delete+insert，单测 + e2e `t_wk` 覆盖）。单测 `TestDecidePlan` 扩至 19 例；e2e 新场景：`--key` 指 NOT NULL UNIQUE → 行级 UPDATE 收敛、键值变化 → delete+insert、`--where` + 非唯一键 → exit 3 且 dry-run 与 `--apply` 下 dst 零写入（HEX 全表断言）。
- **P0-2（生成列）**：`IntrospectTable`/`IntrospectStructure` 读 EXTRA 识别 `VIRTUAL GENERATED`/`STORED GENERATED`（5.7/8.0/TiDB 拼写一致；`DEFAULT_GENERATED` 不匹配）+ 结构路径 best-effort 读 GENERATION_EXPRESSION。**结构同步安全拒绝**：源表含生成列 → 建表计划与结构 DDL 均报运行错（exit 2，零写入，提示手动对齐或 `--no-sync-schema`）——生成表达式是跨后端语义承诺，复现不忠实；两侧定义一致时不算漂移。**数据路径只比较不写入**：`Builder` 新增 writeCols/writeIdx 可写子集（生成列留在 Cols 参与比较、排除出 INSERT 列清单与 UPDATE SET；生成键列仍可进 WHERE）。单测 `TestBuilderGeneratedKeyColumn`/`TestBuilderCompositeGeneratedKey`/`TestAddableGenerated` 等；e2e `t_gen` 新场景：生成列值漂移同步后收敛到源值 / dst 丢生成列 → 结构同步拒绝（dry-run 与 apply 均 exit 2、数据原样）。
- **P0-3（写路径参数化）**：DSN 显式 `interpolateParams=false`（驱动默认，写明意图）。`Builder` 新增 Exec 方法族（`InsertExec`/`InsertBatchExec`/`UpdateExec`/`DeleteExec`）：占位符 + 驱动绑定，值永不上 SQL 文本；字面量渲染器只留作 dry-run 展示与读侧谓词。`bindArg` 按家族转换（DECIMAL/ENUM/JSON/BIT 的 []byte 按字符串/数值绑定，超 int64 的 uint64 转十进制串，BIT 转十进制值）。`Applier` 的 ApplyOps/streamChunk 全部改走 Exec 方法。e2e 新场景：dst 全局 `NO_BACKSLASH_ESCAPES`，值 `C:\abc\def`、`a\b`、`'`、`a'b`、`{"path":"C:\\abc"}`、`中文\测试` 经 sync 写入后 **HEX 全表字节级比对**一致；dry-run 展示 SQL 与执行 SQL 分离（展示仍为字面量，执行全绑定）。
- **P1-1（显式 `--key` 指到 PK/NOT NULL UNIQUE 识别唯一）**：见 P0-1（`ExplicitKeyIsUnique` + `Schema.KeyIsUnique` + `applyKey` 的逐侧 resolver）；`TestExplicitKeyIsUnique` 系列单测钉死（主键/NOT NULL UNIQUE/可空 UNIQUE/普通索引/复合成员）。
- **P1-2（每条实际 scan 连接 applySession）**：`OpenSide` 的 pre-warm 只作延迟优化（database/sql 会把同一条空闲连接发还给连续 `Conn()` 调用，parallel>1 时远不能覆盖全部物理连接）；正确性机制移到 `AcquireScan`：`inited map[CONNECTION_ID]bool`（**不能用 *sql.Conn 指针**——每次 `DB.Conn()` 新建 wrapper，指针永不相等、memo 变死代码；CONNECTION_ID 在物理连接生命周期内稳定）+ `initMu`，首检连接先 `SELECT CONNECTION_ID()` 再决定要不要跑只读 SET 批（幂等，不在锁内发 SET）。e2e 探针改为 **general_log 实证**（performance_schema.session_variables 只能读当前会话，跨会话不可读——实测踩坑）：并行扫描期间快照 general_log 到临时表、关 log、断言 ≥2 条 scan 连接都执行了只读初始化且无一漏设；`--allow-unenforced-readonly` 时断言未强制。
- **P1-3（结构漂移默认不 TRUNCATE）**：`ApplyStructureTable` 改为**原地 ALTER**（InnoDB ALTER 原子：失败 = 表原样，不半迁移不清空），之后重读元数据重比较重规划（修回键 → 行级；仍无键 → 全量重灌，`TRUNCATE` 只在确认全量重灌前发生）；原地失败默认 **FAILED + 数据保留**（exit 2，报错指名 flag），`--allow-structure-truncate` 才回退"先 TRUNCATE，再按两侧重新 introspect 重规划 DDL（不重放旧计划）"。dry-run 展示同步改（行级不再显示 TRUNCATE，全量才显示）。`convergeAfterDDL` 的 RowOps 用真实 `fres.DstRows`（原地 ALTER 后 dst 仍有行，空 src 配非空 dst 必须按键删）。e2e `t_structfail` 新场景：dst 数据装不进 src 列类型 → 默认报错 + 数据保留（内容断言）、加 flag 回退 TRUNCATE 重灌收敛。
- **P1-4（唯一值互换）**：`Engine` 新增 uniqueCols（两侧主键/唯一索引覆盖列，保守超集）；块内 `diffUniqueProtected`：任一被写值被本块另一行持有（swap/环/越序）→ 整块两相重发（全 DELETE 先行、再全 INSERT，转换的 update 含在内），持有被写值的 no-op 行 **rewrite**（opRewrite：删整组 + 重插同值行，释放唯一槽不改数据；delKeys 覆盖组内**全部**行的原始键——规范化折叠的组内原始键可不同，只删首行会留残）。跨块互换（值的所有者在 A 块、当前持有者在 B 块）逐块事务排不出安全序 → 升级 FULL 全量重灌（`srcWriteOwner`/`crossChunkHeld` 双 pass 检测，e2e 覆盖块内 swap 收敛 + 不升级全量）。`Counts` 把 rewrite 计为删 len(delKeys) + 插 len(rows)。
- **P1-5（`--snapshot` 每侧一连接一事务）**：`Planner` 的查询走新 `Querier` 接口（*sql.DB 或 *sql.Conn）；比较器 `beginSnapshotTx` 每侧开一个读事务，COUNT、键极值（Extremes）、切块规划与全部行扫描都在该侧同一事务/同一快照连接上完成。文档（README/MANUAL）明确一致性是**单侧**的（两侧各读各的时间点，不跨侧对齐）。e2e `t_snap` 新场景：并发 writer 持续改数，snapshot 扫描期间结果自洽（无半更新状态、exit 稳定、复跑收敛）。
- **P1（BIGINT 极值 overflow-safe）**：`spanSafe` 拒绝 hi-lo 溢出 int64 的跨度（负 lo 且范围宽于 MaxInt64，含 MinInt64..MaxInt64 全跨度 2^64 值）→ 采样切分回退；`intBoundaries` 全部算术移入 uint64（span<2^64 时精确），边界先 clamp 进 [lo,hi] 再回转 int64；`n > MaxInt32` 也走采样（防 i*step 绕 uint64 环）。单测 `TestSpanSafe`/`TestIntBoundariesExtremeSpan` 等；e2e `t_bigint`（-9223372036854775808 ~ 9223372036854775807 极值 5 行）diff 一致 + sync 后极值行恢复。
- **P2-1（`--sample-limit 0` 显式零值）**：`Options.SampleLimit` 改 `*int`（nil = 未设取默认 5；显式 0 = 不展示示例，计划与计数照常）+ `Config.SampleLimitOr`；CLI 用 `Flags().Changed` 区分显式 0。e2e 断言 `--sample-limit 0` 保留计划、无示例 SQL。
- **P2-2（GitHub Actions CI）**：`.github/workflows/ci.yml` 四任务——unit（lint + test + Windows 交叉编译检查）、mysql8-e2e（build + e2e 脚本自带容器）、mysql57、tidb（各自 compat 脚本）。
- **P2-3（AUTO_INCREMENT 能力判定去全局化）**：旧 `stateReconcilable` 用**一张**随机自增表的 gap 判整个后端（TiDB 分配器误伤普通表；普通 MySQL 有人把计数器抬得比数据高很多会被误判成分配器）。改为逐侧 `stateExactness`：VERSION 含 tidb / `@@tidb_alloc_chunk_size` 可查 → 非精确（一次性告警、该侧跳过）；纯 MySQL/MariaDB 版本号 → 精确（计数器抬高属状态分歧而非能力限制）；都无法分类 → 未知后端**逐表**按 gap 判定（`sideStateExact`，一张表的 gap 不再否决整库）。`checkState`/`VerifyState` 改走 `stateValues`（读不到 / 无自增列 / 非精确值 → 跳过而非失败）。TiDB 套件实测：分配器 skip 场景 + "sync 仍干净跑完（exit 0）"断言。
- **review 发现并修复（本轮内闭环）**：① `NewBuilder` 键列按 Cols 序配 keyIdx，复合键序与列序不同（PK (b,a) over (a,b)）时 WHERE 值互换 → 改按**索引序** + `TestBuilderKeyIndexOrder`；② opRewrite 只删首行键（组内原始键可不同）→ delKeys 全组；③ `inited` memo 用 *sql.Conn 指针（永不相等，死 map + 每 checkout 重跑 SET）→ CONNECTION_ID 键控；④ `PlanTable` 的 `--where` + errEscalateFull 仍展示 TRUNCATE 而 apply 会拒 → dry-run 镜像拒绝（FAILED，exit 2）；⑤ 两侧键**不同**（名称/顺序，如 PK (a,b) vs (b,a)，`--no-sync-schema` 下可过 pre-pass）时 src 键界渲染到 dst 错位列：compare 侧 `sameKeySequence` 不一致 → keyless 全表多重集回退（comparer + drilldown 双修，drilldown 的 keyed join 会配错行）；sync 侧 `keyAgree` 不一致 → FULL（outOfRangeDeletes 已有 guard 双保险），配 `--where` → 参数错（"SAME usable key"）；⑥ e2e 种子 `m_swap.sql` 两步互换撞唯一索引 → 三步经临时值；`m_gen_drift.sql` 的 `DROP COLUMN IF EXISTS` 非 MySQL 语法 → 裸 DROP。
- **结果**：单测全绿（含 `-race`）+ gofmt/vet 干净；主 e2e **229** / 5.7 **89** / TiDB **86+1 skip** 全部通过（本条为代码改动后复跑）。
- **已知限制（非本轮引入，文档化）**：`--ignore-columns` 忽略键列时 filterIgnored 不修剪 Key 列表（比较侧渲染不存在列 → 只读报错，非写风险）；PolarDB-X 未实测；1 亿行为线性外推（10M 已实测）。
- **生产建议**：默认 dry-run 零写入、apply 前交互确认、写后自动复验、只读护栏全连接覆盖（e2e 实证）——在 MySQL 8 / 5.7 / TiDB 上可按正常流程使用 `--apply`；生成列表、非唯一 `--where` 键、结构 ALTER 失败等高风险边界均有显式拒绝或数据保留，无静默路径。

## 更新（2026-09-04）：第二轮 review 修复轮（P0-1/2、P1-1~6、P2-1~4）

- **背景**：基于 `security-fixes` 分支（`b469d92`，已推远程）继续修第二轮 review 发现的问题。目标：先把数据安全、正确性和 1 亿级内存边界修稳；**不新增** watermark/checkpoint/CDC 等新功能，**不做大范围重构**。原则不变：src 永远只读 / dst 仅 `sync --apply` + 用户确认后允许写 / dry-run 零写入 / 默认优先 row-level / 不能证明安全时明确拒绝，不要猜 / 大表内存保持 O(chunk) 或 O(chunk+delta)，不能 O(table)。
- **P0-1（读侧谓词全参数化，不再堆转义规则）**：新增 `type Pred struct{ SQL string; Args []any }` 贯穿读路径——chunk 键界（Lo/Hi，含 lead-prefix 复合键形态）、OOR 严格比较（`RenderLessThan`/`RenderGreaterThan` 改走绑定参数）、采样切分点查询、跨块持有者查询（`holdersOf`，`IN` 式 OR 批 200）全部参数化；`--where` 保持用户原始 SQL（文档写明）。键形态覆盖全部类型家族（int/uint/year/date/datetime/timestamp/decimal/char/enum/set/binary/bit，复合键逐列）。e2e 新表 `t_strkey`（VARCHAR(128) PK，12 个对抗值：`a\b`、`a\\b`、`a'b`、`中文\测试`、`C:\abc\def`、`z\末`、`p\q`、`x'y`、`m\n`、`o'p`、`尾\部`、`末\z`），小 chunk-size 强制多块 + 键界落在值内部，dst 开 `NO_BACKSLASH_ESCAPES`，diff / sync / 范围外删除 / 字符串键采样全过，最终 src/dst **HEX 全表字节级一致**。
- **P0-2（唯一值互换默认拒绝 destructive rewrite）**：唯一键上的 swap/环/holder 不再默认自动 DELETE+INSERT（FK ON DELETE / trigger 副作用不可证明安全）。默认**显式拒绝**（exit 2，报错指名 `--allow-row-rewrite`）；加 flag 才允许；dry-run 计划中此类 op 单独标注 `DESTRUCTIVE ROW REWRITE`。e2e：两行互换 → 默认拒绝且 dst 原样、三行环 → 拒绝、holder → 拒绝、普通单行 UPDATE 不受影响仍走 UPDATE；FK `ON DELETE CASCADE` 表实证默认路径**从不**级联删子行。
- **P1-1（生成列比较含表达式）**：两侧生成列定义比较纳入 `GENERATION_EXPRESSION`：`normalizeGenerationExpr`（仅 trim + 剥最外层括号，**不做 AST 改写**；归一后不同 → 结构漂移 + 拒绝同步；VIRTUAL vs STORED 不一致 → 拒绝；表达式读不到 → 安全拒绝而非猜测）。e2e `genx`（表达式漂移）/`genv`（VIRTUAL↔STORED）双拒绝场景 + 正常生成列回归。
- **P1-2（多 DDL 部分失败后不信旧计划）**：任何结构 DDL 失败后**永不重放旧计划**：默认路径停下报错（"one or more prior DDL statements may already have been applied; destination data was not truncated"，重跑会从当前 schema 重新规划）；`--allow-structure-truncate` 路径 = 失败 → TRUNCATE → **两侧重新 introspect → 重新 diff → 只执行仍需要的 DDL** → 复验。单条 ALTER 失败沿用原子回退语义（"rolled back atomically"）。e2e `t_mddl` 双 DDL 表：DDL#1 成功 DDL#2 失败 → 默认 exit 2 + 数据保留（内容断言 count==4）→ **重跑同样 exit 2 且不重复应用 DDL#1**（count 不漂移）→ flag 路径 TRUNCATE 重灌收敛（count==3、distinct 值==3）。
- **P1-3（scan 连接初始化时序）**：`AcquireScan` 在 `applySession` **成功后**才把 CONNECTION_ID 记入 `inited`（失败不留 memo，下次 checkout 重新初始化）。
- **P1-4（`--snapshot` 严格性）**：快照事务必须落在 REPEATABLE READ + `START TRANSACTION WITH CONSISTENT SNAPSHOT` 上；后端不支持 CONSISTENT SNAPSHOT（实测 RC 全局下 TiDB/部分层）→ **显式拒绝**，绝不静默降级到普通快照或当前读。e2e：dst 全局 `READ-COMMITTED` 下 `--snapshot` 仍干净出 diff（exit 1，不是报错）、复位后收敛（exit 0）。
- **P1-5（唯一约束元组化）**：`UniqueConstraint{Name, Cols}` 替换扁平 UniqueCols——复合 `UNIQUE(a,b)` 的成员列不再各自被视为唯一；冲突判定 = **约束身份 + 完整元组**；可空唯一列的 NULL 不占槽；跨约束不互撞。`conn.UniqueConstraints` 重构为 `accumulateUniqueConstraints`（逐行归组）——顺带修掉 review 轮首跑 e2e 暴露的**真实 panic**：旧代码 `var cur int` 零值使首条唯一行 `cons[0]` 越界（单测从未覆盖，逻辑内联在 DB 查询里）；新 `TestAccumulateUniqueConstraints` 含"单 PK 行"精确复现形。e2e `t_comp`/`t_two`/`t_nu` 回归。
- **P1-6（跨块互换检测 O(chunk+delta)，不是 O(table))**：只跟踪**实际写入过的**元组（`writtenTuples`，上限 10000 提前退出——超过即安全拒绝/升级全量，不继续攒）+ 定向参数化 holder 查询（`holdersOf`）。`classifyHolder` 门序：本块已写入键 → 安全；块内未处理持有者 → 冲突；**OOR 集合成员 → 安全**（OOR 扫描是服务端权威，先于 chunk 执行，不依赖客户端排序）；`!oorActive` → 冲突；`!orderKnown` → 冲突；orderKnown 且界外 → 冲突；界内 → `holderInOtherChunk`（src 侧 LIMIT 2 定向查）。`keyOrderKnown` 白名单：int/uint/year/date/datetime/timestamp/bytes/bit 家族 + 仅 `_bin`/BINARY 字符串键（ci 排序、DECIMAL/FLOAT 变宽数字串、TIME 变宽小时、ENUM/SET 按标签、JSON 一律不可证明 → 拒绝而非猜）。基准：1M 写入 op 跟踪 ≈ delta（~13ms / ~2MB，不随表规模增长）。单测 `TestKeyOrderKnown` 19 例（含复合键含 decimal 成员 → false）、`TestClassifyHolderOorSet` 6 例（含 ci 键 OOR 安全 / ci 键界内冲突 / OOR 残留冲突）。
- **P2-1（每 worker 一条 scan 连接）**：非快照并行 diff 中每 worker `AcquireScan` 一次、复用到底、结束 Close；pinned 连接致命错误 → 重取新连接重试一次（DB 级方法的一次自动重试对 pinned conn 不存在）。配套修 **pre-warm 真实预热**：旧 pre-warm 循环每次 `Conn()` 从空闲 FIFO 取回**同一条**连接（实测一条连接被初始化 `parallel` 次、其余连接靠惰性首用补）→ 改为持有全部 `parallel` 条直到全部打开再统一归还（每连接恰一次幂等 SET 批，文档注释同步更新）。
- **P2-2（采样切分点带 `--where`）**：`sample()` 切分点查询谓词并入 `p.Where`（稀疏过滤表按**过滤后**的行切块，不再按全表位置切出空块）。e2e `wheresparse`：稀疏过滤下漂移检测 → 收敛 → 复验一致。
- **P2-3（占位符上限）**：`const maxBindParams = 60000`；`effectiveBatch = min(configuredBatch, maxBindParams/writableColumnCount)`；单行超上限 → 显式报错（不静默截断）。e2e `t_wide`（120 列）：10 行×1000 批正常、100 行×1000 批自动收缩、单行 >60000 占位符报错路径。
- **P2-4（原子性表述修正）**：单条 InnoDB `ALTER TABLE` 原子；**多条** DDL 批**不**原子。代码注释、报错文案、README/MANUAL 中所有"整轮 structure sync 原子"类表述清除，改为逐条准确表述（单条失败原子回滚 / 多条部分成功 → 重规划路径，见 P1-2）。
- **review/实测中修掉的新问题（本轮内闭环，不单独成项）**：① 上述 `accumulateUniqueConstraints` 越界 panic（P1-5 条内）；② OOR 集合 / `orderKnown` 门序重构——旧门序让 ci 排序键的**合法**跨块持有者（值将先被 OOR pass 删除）被误拒 → `RowOps` 把 OOR 扫描移到 chunk 循环之前、`oorSet` 由 OOR op 键构建、`classifyHolder` 门序重排（OOR 集合成员先于 orderKnown 判定），dry-run 计划验证 `DELETE v='Z'` 排在 `INSERT` 之前；③ e2e 测试数据修正：`m_xchunk_swap` 旧版把行 12 改成**源表不存在**的值（不是互换、唯一 holder 在更早块、本就该安全）→ 改为真跨块互换 `'v1'↔'v12'`（行 1 ∈ 块[1..6]、行 12 ∈ 块(6..12]，chunk-size 10），末态断言同步改；`m_nu_change` 旧版在可空唯一列上做值互换（触发 P0-2 拒绝，与场景 `expect 1` 矛盾）→ 改为两个普通值变更；④ **P1-2 general_log 探针适配参数化读**（P0-1 的连带）：参数化查询走 `COM_STMT_PREPARE/EXECUTE`——general_log 记录其命令类型（Prepare/Execute）但**不含语句文本**（慢日志同样不记，实测确认），旧探针按 `Query` 文本匹配 → 只数到控制连接 1 条 → FAIL。重设计：scan worker 按 Prepare/Execute **量**识别（每 worker ~250 块对，控制连接 <20，阈值 50），经典读（COUNT/键极值/下钻）按 `Query` 文本匹配，两路 UNION；断言 ≥2 条读连接 + 全部带只读初始化 + 输出含 `comparing 1000 chunks` 行（把"扫描确实发生"绑进断言）；setup 加 `DROP TABLE IF EXISTS` 防残留表撞 CTAS。实测：5 线程（控制 + 4 worker）、unenforced=0。
- **内存结论**：除 `dstDeletes`（dst 侧按键匹配删除，O(匹配 dst 行数)，已加内存注释：键定址删除、安全但**非**内存有界，流式化留作后续——本轮禁大重构）外，diff/sync 全路径 O(chunk) 或 O(chunk+delta)；P1-6 跟踪结构不再随表规模增长（基准实证 ≈ delta）。无 O(table) 残留结构。
- **对抗性自查（实现完成后逐项）**：dry-run 零写入（`cmd/mtdiff/sync.go` dry-run 分支在 `OpenWriter` 之前返回）、src 永不写（写连接只对 dst 建立）、生成列排除出写列、P1-2 多 DDL 重规划不重放旧计划、P1-4 快照门不静默降级、OOR 删除排最前（`append(oor, out...)`）、P0-2 默认拒绝在 `ApplyOps` 之前返回（零写入）、P1-6 溢出/不可证明路径全部走拒绝或显式 FULL 升级——未发现假绿/误删/跨 `--where` 写/src 写/dry-run 写/FK 触发器副作用路径。
- **结果**：单测全绿（含 `-race`）+ gofmt/vet 干净；主 e2e **295**（182 退出码 + 113 输出内容，229→295）/ 5.7 **89** / TiDB **86+1 skip**（三套均为本轮代码复跑，compat 场景集未扩）。
- **生产建议**：见上轮——本轮之后 `sync --apply` 的高风险面（字符串键边界、唯一互换、多 DDL 部分失败、快照严格性）全部有显式拒绝或数据保留路径，可按正常流程使用；`--allow-row-rewrite` / `--allow-structure-truncate` 两个破坏性开关默认关闭且报错文案指名。

## 更新（2026-09-04）：第三轮 review 修复轮（确认作用域门 / 流式删除 / 批量 DELETE）

- **背景**：基于 `security-fixes`（`d12a96f`，已推远程）继续修第三轮 review 问题。安全不变式：src 永远只读 / dry-run 零写入 / dst 仅 `--apply` + 用户确认后写 / **用户确认后不允许程序扩大破坏性作用域** / 不能证明安全就拒绝 / row-level 不得偷偷升级为 TRUNCATE / 大表内存 O(chunk) / **不允许 1 亿行保存 1 亿个 key/op/map 条目**。不新增 watermark/checkpoint/CDC，不做大重构。
- **P0（确认 vs 实际破坏性计划的作用域一致，确认后不可扩大）**：新增 `DestructiveScope{RowRewrite, FullResync}` + `TableSync.Rewrites`；preflight（完整 `PlanTable` 含 `PlanRowWork`）在**确认提示之前**把确认计划的破坏性作用域记入确认文本与确认计划。apply 时重新规划（fresh），经唯一门 `scopeGate`：FULL 而确认计划未显示 TRUNCATE → 拒（`ErrReplanRequired`，表停、**零 TRUNCATE**、exit 2）；ROWLEVEL 重写组数 > 确认数 → 拒（报错点名"re-plan needs N destructive row rewrite group(s)，确认计划显示 M"）；收缩（实际 < 确认）恒允许。ROWLEVEL **永不**在同一次 apply 内升级 FULL/TRUNCATE：`errEscalateFull` 两处门（数据路径 `convergeAfterDDL` + 结构路径）确认计划未显示 TRUNCATE → STOP + FAILED + 零写入；确认计划显示了（显式 FULL）才执行。`--allow-row-rewrite` **只授权 DELETE+INSERT 重写**，从不授权 FULL/TRUNCATE（跨块冲突 + flag → 计划变 FULL，仅当确认计划显示 TRUNCATE 才执行；不设独立 `--allow-full-resync`——确认门已覆盖）。单测 `scope_test.go`（4 组：ROWLEVEL 永不 truncate / 重写计数门 7 例 / 报错文案点名差异 / FULL 显示未确认）。
- **P0-3（破坏性重写在确认前可见）**：确认提示前完整 preflight（多一次扫描可接受，安全优先）——全部 `DESTRUCTIVE ROW REWRITE` 条目与 TRUNCATE 样本先于确认出现在用户看到的计划里。
- **P1-1（生成表达式不可读 → 保守拒绝）**：任一侧 `GENERATION_EXPRESSION` 读不到（NULL/空）→ 安全拒绝 "generation expression unreadable on one or both sides; schema equality cannot be proven safely"（旧行为 `""==""` 假绿）。单测 4 组合（两侧可读相同 / 可读不同 / 单侧不可读 / 双侧不可读）。
- **P1-2（dstDeletes 流式 O(chunk)/O(batch)）**：dst 侧按键匹配删除不再整表持有匹配键集：逐块 `keyDeleteBatches`（块内按规范化身份排序 + 按 cap 切批）→ 逐批 `DeleteBatchExec`（一批一事务，affected rows 提交后累计）。`Stats.MaxBufferedDeleteKeys` 记录峰值缓冲（实测 = 一个批）。
- **P1-3（outOfRangeDeletes 流式 + 删掉 oorSet）**：OOR 删除路径改 keyset 分页（参数化游标，复合键 lead 展开 `k1 > ? OR (k1 = ? AND k2 > ?)`，NULL 端 `IS [NOT] NULL` 分支，无 OFFSET），逐页删除，内存 O(batch)；客户端 `oorSet`（O(越界行数)）**删除**——改 holders 查询上的**服务端 flag 列**（OOR 谓词渲染为 SELECT 里的 0/1 列，`classifyHolder` 读 `h.oor`：越界由服务端证明，不依赖客户端排序或驻留集合）。
- **P2（批量参数化 DELETE + 不扫全键集）**：`DeleteBatchExec`（单列 `IN` + NULL `IS NULL` / 复合键 OR-of-AND，全绑定；空列表报错、keyless panic）；`deleteBatchCap = min(batch, maxBindParams/keyCols)`（下限 1）；`runOpsGroup` 删除累积 + 屏障语义（opUpdate/opRewrite 先 flush 删除；opRewrite = 删组 + 重插）。dry-run 不扫全键集：COUNT + 有界样本 + 块级批量删，显示 `STREAM DELETE ~N` / `STREAM DELETE N out-of-range … (keyset-paginated batches, committed before the in-range writes)`。
- **实绑顺序 bug（e2e 实测炸出，本轮修掉）**：OOR flag 列在 SELECT 列表里，其 `?` 文本上**先于** WHERE 子句，而驱动按文本出现顺序**位置绑定** → 旧代码把 flag args 追加在 tuple args 之后，绑定整体错位：无害的 3 行更新表被误判"跨块唯一互换"（假阳性），其他形态可能漏掉真 holder（假阴性）。修：flag args **领跑**参数列表（`holdersQuery` 契约注释 + `TestHoldersQueryArgOrder` / `TestHoldersQueryCompositeFlagOrder` 回归钉死）；`keyset_test.go` 7 例钉死游标 SQL 形态（单列/复合/NULL 头/NULL 尾/全 NULL）。
- **内存基准（合成键，3 次均值）**：`BenchmarkKeyDeleteBatches1M`：峰值缓冲 = 一个 chunk（100k，断言上界成立）、批 ≤ 500、~3.0 allocs/row；`BenchmarkKeyDeleteBatches10M`：峰值仍为一个 chunk、~3.0 allocs/row——**数据 ×10，内存不 ×10，上界恒为一个 chunk**；`BenchmarkDeleteBatchExec500`：82µs/批、27.7KB/批。
- **19 项测试映射**：1-2/4 → `scope_test.go`；3/6 → e2e t_swap/t_u3/xchunk（既有，本轮复验）；5（stale plan）→ `errEscalateFull` 门 + `scopeGate`（preflight→apply 数据竞态无法确定性 e2e，单测 + 代码门钉死）；7-10 → `struct_test.go` + e2e genx/genv；11-14 → `MaxBufferedDeleteKeys` 仪表化 + `keyset_test.go` + 1M/10M 基准；15-18 → `delbatch_test.go`（含对抗值字节级、NULL、复合键、占位符上限 60000）；19 → `TestRunOpsGroupMidStreamFailure`（执行器级：当前批回滚、已完成保留）+ e2e 收敛场景（复跑收敛 = 语义级）。
- **e2e/compat 脚本修正（流式化连带）**：5 个主 e2e 场景 + 1 个 compat 场景中越界删除由逐行 DELETE 样本变为单条 `STREAM DELETE` 计划行 → 断言改 `STREAM DELETE`（m_sync_more / t_oor / m_oor_converge / t_plain stray / t_swap m_swap_addr——"删除先于插入"的保证改断言行内措辞 `committed before the in-range writes`）；m_sync_more.sql 注释 off-by-one 修正（实际 500 行 5001..5500，1500 vs 1000）。
- **自查 grep（逐项）**：`errEscalateFull` 5 处全部有作用域门；`TRUNCATE TABLE` 仅 4 类站点（FULL 路径 / 结构 flag 回退 / convergeAfterDDL 门 / 展示）；`AllowRowRewrite` 仅规划期语义；`map[string]bool` 全部小（cols/seen/targeted）；`scanKeyRows` 仅 chunk 级；无 `dstDeletes`/`outOfRangeDeletes`/`oorSet`/`RowOps`/`PlanSummary` 残留；GenExpr 拒绝 3 处。
- **结果**：单测全绿（含 `-race`）+ gofmt/vet 干净；主 e2e **296**（295→296，新增 OOR 流式计划行断言；5 个旧 DELETE 样本断言改为 STREAM DELETE 计划行断言）/ 5.7 **89** / TiDB **86+1 skip**（三套均为本轮代码复跑）。
- **内存结论（1 亿行边界）**：两条流式删除路径（dstDeletes / OOR）O(chunk)/O(batch)（仪表化 + 1M/10M 基准证上界 = 一个 chunk，与 N 无关）；跨块跟踪结构 O(delta)（cap 10000 提前退出）。**残留 O(delta)**：普通 RowOps 本身按块组保留 ops（insert/update/delete/rewrite）——**完全分歧**的 1 亿行表（每行值都不同）会保留全部行的 ops，内存正比于差异量 delta 而非 chunk。"支持 1 亿行"的准确边界：**相同/近相同大表（delta ≪ N）O(chunk) 内存，可支撑；完全分歧大表内存正比于差异**。
- **生产建议**：确认后作用域不可扩大（门 + 单测钉死）、两条删除流内存有界（基准实证）、破坏性 op 确认前可见——`sync --apply` 可按正常流程使用；1 亿行表先跑 dry-run 看计划（delta 大时内存按 delta 计），完全分歧的大表建议分批（`--where` 按键范围切段）逐段同步。

## 更新（2026-09-05）：第四轮 review 修复轮（破坏性作用域一致性 4 项）

- **背景**：基于 `security-fixes`（`fa2d6c4`，已推远程）继续修第四轮 review 的 4 个安全/一致性问题。不变式不变：src 永远只读 / 破坏性操作失败关闭（fail closed）/ preflight→apply 之间数据或结构变化**不得**扩大已确认的破坏性作用域 / 用户确认的是**具体作用域**（哪几组、哪个 TRUNCATE），不是"至多 N 个操作" / DROP/TRUNCATE/rewrite 基于 apply 时刻的新鲜事实 / 元数据查询异常 → 对应破坏性写入不执行 / 新写路径全参数化。禁大重构、禁改 CLI 语义与退出码体系、禁降安全上限、禁删既有安全回归。
- **R4-1（确认摘要跟随 resolved config）**：`syncSummary()` 旧代码读 CLI 全局 `syncOpt.structTruncate`——YAML `allow_structure_truncate: true` + 不带 flag 时，executor（走 resolved `cfg.Opts.AllowStructureTruncate`）会 TRUNCATE 而摘要不警告。修：`syncSummary(plans, allowStructureTruncate bool)`，调用点传 `cfg.Opts.AllowStructureTruncate`（executor 与摘要**同一个 resolved 值**）；显式 CLI flag 覆盖 YAML（与所有选项一致的优先级，`Flags().Changed`）。单测：resolved true→注记在 / false→不在；**YAML true + flag 未设→显示**（报告缺陷本体）/ 显式 false + YAML true→不显示 / 显式 true + YAML false→显示（`TestSyncSummaryStructureTruncate` + `TestSyncSummaryResolvedConfigResolution`）。
- **R4-2（DROP TOCTOU 重校验）**：整库 DROP 计划在 pre-pass 发现"dst 独有表"，确认后 `ApplyDrop` 直接 `DROP TABLE IF EXISTS`——src 在确认与执行之间建了同名表也会把 dst 表删掉。修：DROP 前**重新查询**两侧（fail closed）：① src 现在**有**该表 → 拒（`ErrReplanRequired` 语义：表停、FAILED、**DROP 未执行**、dst 未动，报错含 "source table … appeared after the plan was confirmed / DROP was not executed / Re-run"）；② 任一侧元数据查询失败 → fail closed 拒；③ dst 表已不在（带外消失）→ 目标态（不存在）已达成 = 收敛 APPLIED/OK，零执行；④ 两侧事实与确认一致 → 正常 DROP + 既有 post-DROP 验证。测试缝：Runner 三个 nil-prod hook（`srcDropRecheck`/`dstDropRecheck`/`dstDropPostcheck`）+ Applier `execHook`（单测数 DROP 执行器**调用次数**，不只断言报错文本）。单测 5 例（`drop_test.go`：src 出现→拒且 0 次调用 / 两侧元数据失败→fail closed 0 次 / dst 消失→收敛 0 次 / 正常→恰 1 次 / post-DROP 仍存在→FAILED）。**真实 MySQL 集成**（`integration_test.go`，e2e 的 srcdb2/dstdb2 上跑，无 env 跳过）：`TestDropRaceRealMySQL` 四分支全走真库（正常删 / src 后建→拒且 dst 行存活且内容完好 / src 连接关闭→fail closed / dst 带外消失→收敛）。
- **R4-3（rewrite 授权从数量升级为身份）**：`DestructiveScope.Rewrites` 计数授权不足——重规划可以**数量不变、组变了**（确认 {A,B}，apply {A,C}）。修：每组一个**身份指纹**（`fingerprint.go`）：约束列名 + 组内 dst 键行，逐值**类型标签 + 长度前缀**规范编码（int64/uint64/float64/string/[]byte/time/bool/nil 互不碰撞，未知类型带标签回落），行身份**排序后** sha256（行序/ map 序不改变指纹；摘要单向，不泄露业务值；`int64(5)`/`"5"`/`5.0`/`0x35` 四值恒不同组）。`DestructiveScope` 增加 `RewriteFingerprints []string`（`TableSync.Rewrites` 保留作显示）；`rowWork.rewriteFPs` 从 opRewrite 收集；`scopeGate` 重写轴改**子集判定**（确认组的全部/部分/无 → 放行；任何确认外组 → 拒，报错只给 confirmed/current/new **计数**，绝不打印键值）；`convergeAfterDDL` 结构路径的 `rewrites>0 && !scope.RowRewrite` 检查保留。单测：`TestScopeGateRewriteIdentity` 11 例（同组/乱序/收缩/空放行；**同计数换组 {A,B}→{A,C} 拒**、{A,B}→{C} 拒、超集拒、无确认有重写拒）；`TestFingerprint*` 8 组（行序置换确定性、复合键 (1,2)≠(12)、特殊字符 `'` `\` 换行 NUL 中文 全互异、NULL≠空串、uint64 max≠int64 -1、类型注定性、约束身份入指纹、子集/新增计数）。**真实 MySQL 边界说明**：opRewrite 组（no-op 行持有被写元组）在**一致且约束被强制执行**的 src 上不可构造（no-op 行 src=dst 同值，写者 src 行同值 ⇒ src 自违反唯一约束；e2e 的 t_swap/t_u3 场景实为 hot 两相 DELETE+INSERT，rewrites=0）。故组身份门以单测钉死逻辑；真实库侧改测**可达的同类门**——`TestScopeEscalationRealMySQL`：确认 ROWLEVEL（无 TRUNCATE），确认后 dst 被改成**跨块唯一互换**（chunk size 1），apply 重规划升级 FULL → 拒（"full resync … confirmed plan did not show"，表停、dst/src 零写入逐行断言），还原后同一确认计划收敛（收缩恒允许）。
- **R4-4（严格 YAML）**：`yaml.Unmarshal` 静默忽略未知字段（`exclude_table` 拼错 → 表悄悄进入同步/删除集合）。修：导出 `config.Parse`——`yaml.NewDecoder + KnownFields(true)`（任一级未知字段报错并指名）+ **拒绝多文档**（第二个 `---` 文档直接拒，fail closed 而非半应用）；`${ENV}` 展开仍在解析前（行为不变）。`LoadFile` 走 `Parse`。单测（`config_test.go`）：合法全字段配置可解析；`srcc`/endpoint `hostname`/`exclude_table`/`allow_structure_trancate`/`allow_unenforced_readonl` 五个未知/拼错字段各自报错指名；合法文档 + 第二文档拒（"multiple YAML documents"）；`LoadFile` 严格 + env 展开共存。
- **结果**：单测全绿（含 `-race`）+ gofmt/vet 干净；主 e2e **297 ok 行（184 退出码 + 113 内容，296→297 = 新增真实库 re-gate 回归行）**——新增 `(0)` 节：e2e 的 srcdb2/dstdb2 上跑 Go 集成测试（`TestDropRaceRealMySQL` 四分支 + `TestScopeEscalationRealMySQL` 升级拒绝 + 收缩收敛），宿主机无 Go 工具链时显式 note 跳过；5.7 compat **57 场景全过**；TiDB compat **全过 + 1 skip**（auto-increment 预分配已知降级）。CI 四任务待 push 后确认。
- **残留风险（如实）**：① `--parallel 1`（池=1 条 scan 连接）+ 需 holder 查询的表 → `holdersOf` 需要同一侧**第二条**连接，`context.Background()` 无期限 → **挂起**（pre-existing，round-3 P2-1 单连接/表模式引入；CLI 默认并行度 >1 不易触发；集成测试用 parallel=2 绕过并注释。下轮候选：holder 查询复用表内连接或加等待上限）。② opRewrite 组真实库路径不可构造（见 R4-3 说明）——身份门逻辑单测覆盖，发射路径为防御性代码。③ 上轮遗留：PolarDB-X 实测、NO_BACKSLASH_ESCAPES quoteString、e2e tz 孤儿 cnf、1 亿行实测。

## 更新（2026-09-06）：第五轮 review 修复轮（parallel=1 死锁 / YAML 结构注入 / make push / scan 会话身份 4 项）

- **背景**：基于 `security-fixes`（`1c31ec9`，已推远程）继续修第五轮 review 的 4 个问题。不变式不变：src 永远只读 / 破坏性 fail closed / 确认后作用域不扩大 / DROP TOCTOU 重查 / rewrite 身份门 / YAML KnownFields 严格 + 多文档拒绝 / 生成列不写 / 值全参数化 / 5.7·TiDB 不回退 / **`parallel=1` 正式支持**。最小改动，不重构 sync engine。
- **R5-1（parallel=1 holder 自死锁，P0/P1）**：根因——`PlanRowWork` 整表 pin 住每侧各 1 条 scan 连接（round-3 P2-1 模式），而 `crossChunkCheck → holdersOf / classifyHolder → holderInOtherChunk → srcRowByKey` 又从**同一侧的池**里 `AcquireScan` 第二条连接；`--parallel 1` 时池只有那一条 → 池阻塞用调用方 ctx（`context.Background()` 无期限）→ **确定性自死锁**（round-4 实测 5 分钟挂起）。修：holder 安全查询**复用 pin 住的连接**——`crossChunkCheck(ctx, srcCn, dstCn *sql.Conn, …)` 直传 PlanRowWork 的 pinned 连接；新增 `holdersOfConn` / `srcRowByKeyConn` / `classifyHolderConn`（Conn 版本，查询严格跑在两次块扫描之间，绝不并发）；**保留** `holdersOf` / `srcRowByKey` 池包装（无 pinned 连接的独立调用方）。全程用传入 ctx，无 `context.Background()` 遮掩。测试：`TestUniqueHolderParallelOneDoesNotDeadlock`（**parallel=1** + ChunkSize=1，10s watchdog 罩住 PrePass/PlanTable：① 安全裁决——dst 行 1 持有 chunk 2 要写的唯一值且排序在前（安全裁决**只能**经 dst holders 查询找到外部 holder + src 点查证明"不同值"才可达）→ ROWLEVEL 计划 + apply 收敛；② 同一对再互换 → holder 排序在后 → 计划升级 FULL（**冲突裁决也到达**，watchdog 内完成）+ 零写入断言）；连接普查（PROCESSLIST 每库恰 = raw 1 + scan 1 + ctl 1 [+ writer 1]，parallel=1 **没有第二条 scan 连接**）；`newE2ERunner` 全切 parallel=1（集成测试现在跑最严配置）并删掉"parallel=2 绕过"注释；e2e `(0)` 节挂上。负向对照（临时测试，跑完删）：pin 住唯一连接后再 `AcquireScan` → 精确阻塞到 ctx deadline（证明 watchdog 能抓到旧行为）。
- **R5-2（YAML `${ENV}` 结构注入，P1）**：根因——旧 `expandEnv` 在 YAML 解析**前**做原文替换：env 值含换行 + `options:\n  allow_structure_truncate: true` 可**注入新配置段**翻动安全开关（KnownFields 管不到，注入的是合法字段名）；`#`/`:`/引号/换行也会破坏值本身。修：替换移到**结构解析之后**——`yaml.Node` 树只走**取值标量**（映射键永不替换；序列项是值）→ 值里每个 `${NAME}` 用 `os.LookupEnv` 替换，**未设置变量 → 报错指名变量名**（fail closed，不再静默空串）→ 原样字节级落回同一值（解析风格保留，re-encode 时含冒号/换行的值自动加引号）→ KnownFields 严格解码 + 多文档拒绝（不变，多文档检查仍在**原始流**上）。`password_env` 行为不变。测试（`config_test.go`）：9 值字节级（`p#ss` / `a: b` / `a"b` / 真换行 / 反斜杠 / 中文 / 字面 `${LITERAL}` 不再次展开 / 真 tab / 单引号）；**结构注入**（多行值含 `options:` 块 → 密码字节级 + 开关保持 false）；未设置变量报错指名；字面 `${}` 不递归；**键永不展开**（键里 `${UNSET}` 走 KnownFields 报字段名，而非 env 错误）；序列项展开。
- **R5-3（make push 安全语义，P1/P2）**：旧 `make push` 三处不安全——默认 `git add -A`（未跟踪文件：密钥/DSN 默认上车）、push 藏在 else 分支（干净但本地领先的树推不出去）、消息文本插进 recipe（`git commit -m "$(m)"`：make 展开后 shell 解析消息里的 `;`/反引号/`$(…)` = 命令注入）。修：① **只 commit 已 staged**（用户 `git add`；无 staged → 不 commit，**绝不** add -A/u）；② **push 是最后一步、必跑**（无 commit 也推既有本地提交；非 git 仓库/索引损坏 rc≠0/1 → 拒绝，fail closed）；③ 消息走**环境变量**（`m="…" make push`）且 recipe 里写 `$$m`——make 把字面 `$m` 留给 **shell** 在双引号里展开（展开结果永不二次解析；实测 make 命令行 `m=` 会被 make 自己的引号规则剥掉双引号，故文档明确 env 形式）：`fix "quote"` / `fix; touch pwned1` / 反引号 / `$(touch pwned3)` / 中文 全部**字面落进提交**，pwned 金丝雀零执行。测试 `scripts/test_make_push.sh`（临时仓库 + bare origin，跑真实 Makefile）：staged→commit+push / 干净+领先→无 m 也推 / 未跟踪 `secret.env` 不进提交且留本地 / staged 但无 m → usage 拒且不推 / 5 个元字符消息字面 + 零执行。挂 CI unit 任务。
- **R5-4（scan 连接策略身份，P2）**：根因——`applySession`（READ ONLY + guardrails）按 `CONNECTION_ID()` memo 每物理连接**一次**；MySQL 重启后 ID 计数器归零，新物理连接可**复用旧 ID** → 新会话（无策略）被 memo 当成已初始化 → 跳过策略 → 违背"每条实际 scan 的物理连接都是策略初始化"。修（安全优先于省 SET）：`AcquireScan` **每次 checkout 重新 applySession**（幂等，成本每 checkout 几条 SET）；策略应用失败 → 连接 close 且**不交出**；删掉 `inited` map / `initMu` / `remember` / `SELECT CONNECTION_ID()`。测试缝：`openDB` 包级变量（生产恒为 go-sql-driver；单测换 fake driver——`fake_driver_test.go`：fake 服务器给**每条物理连接同一个 CONNECTION_ID**（复现 ID 复用）+ 每连接独立会话状态 + 可 kill）。单测 3 例：① **ID 复用**（kill 物理连接 1 → 池开新连接 2，同 ID 7、会话从零开始 → 仍拿到完整策略；旧 memo 代码会在第 2 条上跳过 → 测试变红）② **每次 checkout**（同一物理连接被带外重置回服务端默认 → 第二次 checkout 重新应用）③ **失败不交出**（后端开始拒双 read-only 层 → 已初始化过的连接也拒交 + 替换连接同样拒）。真实 MySQL：`TestRealScanReplacementReinitialized`（KILL 侧库全部物理连接 → 等 PROCESSLIST 消失 → 交出连接必须是**新 ID**（不在 kill 列表）且 `innodb_lock_wait_timeout=5`（服务端默认 50）+ sql_mode 含 NO_ZERO_DATE）。
- **结果**：gofmt/vet 干净；单测全绿（含 `-race`）；主 e2e / 5.7 / TiDB 复跑 + CI 四任务见提交。
- **残留风险（如实更新）**：~~① parallel=1 挂起~~ → **本轮 R5-1 修复**（holder 查询复用 pinned 连接 + 集成测试 parallel=1 全路径 + watchdog；parallel=1 正式支持）。仍残留：PolarDB-X 实测、NO_BACKSLASH_ESCAPES quoteString、e2e tz 孤儿 cnf、1 亿行实测（上轮遗留）；`make push` 的 `m` 若经 make 命令行（`make push m=…`）传入会被 make 引号规则剥掉双引号（文档已明确 env 形式 `m="…" make push`）；YAML `${ENV}` 替换对**锚点/别名**（`&`/`*`）指向的标量不做展开（罕见形态，fail 在 KnownFields 或按字面处理）。

## 更新（2026-09-06）：第六轮 review 修复轮（连接生命周期 3 项 / DSN 语法 1 项 / typed ENV 1 项 / 文档同步）

- **背景**：基线 `security-fixes@e12734d`（round-5，已推远程）。round-5 全部修复不回退（parallel=1 正式支持 / YAML 结构感知替换 / make push 安全 / scan 每次 checkout 重放 / DROP TOCTOU / rewrite 身份门 / 严格 YAML / 生成列安全 / 写路径参数化）。本轮 4 个 review 问题 + 文档同步。
- **R6-1（control 池替换连接必须重放 read-only 策略，P1）**：旧代码 `Ctl()` 直接暴露裸 `*sql.DB`，只在 OpenSide 时对一条 control 物理连接 applySession 一次；该连接被 KILL/断网/重启后由 database/sql 静默替换的新会话**没有任何策略**却继续服务元数据查询。修：删 `Ctl()`；新增 `AcquireControl`（与 AcquireScan 同契约：每次 checkout 重放幂等策略，失败 close 不交出；**dead 连接特例**——策略 SET 报 bad-connection 不是策略拒绝：close 旧连接→新 checkout→**先重放完整策略**→再交出，重试一次；第二次 dead 或真拒绝→报错，绝不出未防护会话）+ `ControlQueryer`（绑定一次 policy-applied checkout；查询遇 dead→`swap`：新连接**先策略后查询**，重试一次；swap 失败 sticky-dead 后续全部 loud fail）+ `WithControl`（acquire→fn→归还前释放——ctl 池单连接，跨同侧二次获取会自死锁，parallel=1 的教训）+ `Queryer` 最小接口（`QueryContext`+`QueryRowContext`，`*sql.DB`/`*sql.Conn`/`*sql.Tx` 均满足）。**全部 ~30 个元数据/规划查询站点从 `*sql.DB` 改为经 control 安全通道**：`schema.go` 18 个函数签名改 `Queryer`（Row 用法改 `OneRow`——QueryContext 基座，dead 连接可透明恢复；Row 的 Scan-time error 不可拦截故规划路径不用 Row）；`chunk.Querier` 改 QueryContext-only；comparer 的 count/plan 走 `side.Control`（snapshot 分支仍走快照连接——死快照事务是硬失败不恢复）；sync/prepare/cmd 全部 `WithControl` 闭包。`DeadConn` 补 `sql.ErrConnDone` sentinel（database/sql 丢弃连接后的报告文本不含 bad/invalid/Lost 字样）；applySession 拒绝错误首层改 `%w`（sentinel 可 unwrap）。测试：fake driver 3 例（**复用 CONNECTION_ID**——服务器重启 ID 归零，新物理连接同 ID：kill 后 control 查询→池开新连接（同 ID）必须**从零重放完整策略**（不能依赖任何 memo）+ 查询跑在新连接上；同一物理连接带外重置→再 checkout 重放；后端拒双 read-only 层→三次 checkout 全拒交+拒交期间零查询（unguardable 连接永不交出））。真实 MySQL：`TestRealControlReplacementReinitialized`（AcquireControl 拿 ctl ID → KILL → 等 PROCESSLIST 消失 → 新 checkout（容忍 ≤5 次 dead）必须是新 ID + lock_wait=5（默认 50）+ NO_ZERO_DATE + `transaction_read_only`（后端暴露时断言）+ TableExists 元数据查询可跑）。
- **R6-2（DSN 不用 net/url.UserPassword 手工拼装，P1）**：旧 `url.UserPassword` 对密码做百分号编码（`s3:cret`→`s3%3Acret`，驱动按字面发 → 认证失败；旧单测甚至把错误值写成期望值）；DB 名 raw append（`db/a`、`db?x` 破坏 path/query 边界）。修：DSN 一律由**驱动自己的 formatter** 生成——`poolConfig` 填 `mysql.Config`（User/Passwd/Net=tcp/Addr/DBName/ParseTime=true/Loc=UTC/Timeout=10s/ReadTimeout=10m/WriteTimeout/InterpolateParams=false/MaxAllowedPacket）+ `cfg.Apply(mysql.Charset("utf8mb4",""))`（正式 charset API，wire 同旧 `charset=utf8mb4`）→ `FormatDSN()`；池经 `mysql.NewConnector(cfg)` + `sql.OpenDB`（每池独立 connector，fresh cfg）。驱动 FormatDSN/ParseDSN 按**最后一个 `/`** 分 dbname、**最后一个 `@`** 分凭据、**第一个 `:`** 分 user/passwd，凭据**原样**写入 → 密码含 `:` `@` `/` `?` `#` 中文均 round-trip（单测 `TestBuildDSN` 用 `mysql.ParseDSN(BuildDSN(…))` 反解析钉死：8 组密码 × 最坏 DB 名 `db/a` `db?x` `db%x` 中文库名，`parsed.Passwd/DBName == 原值` 逐字节；ParseTime/Loc=UTC/charset/timeout 组无回归；writer 600s 写超时同验）。已知驱动边界（注释钉死）：无密码时 username 含 `@` 会丢（驱动格式限制，非本轮引入）。真实 E2E：双容器各建 `mtdiff_special` / 密码 `p@ss:word/a?b`（mysql 客户端 SELECT 1 证账号存在）→ YAML `password: ${MTDIFF_SPECIAL_PASSWORD}` + **typed** `parallel: ${MTDIFF_SPECIAL_PAR}`（=4）跑 diff exit 0（YAML 层字节级 → DSN 层字节级 → MySQL 认证字节级三段闭环）+ 同凭据走 CLI shorthand DSN 再跑 exit 0 + 输出断言密码打码不泄漏。
- **R6-3（writer 替换连接必须重放 guardrails，P2）**：`OpenWriter` 只 applyGuardrails 一次；写连接被替换后新会话没有 lock_wait=5/max_execution_time/NO_ZERO_DATE。修：`Writer.Conn()` **每次 checkout 后** applyGuardrails 再返回（best-effort 语义不变——guardrails 限损不是 correctness gate，失败不报错；但每条新物理写会话都尝试重放）。测试：fake 2 例（kill writer 连接→重新 checkout 新连接必须重新收到全 guardrail；同一物理连接带外重置→再 checkout 重放 lock_wait+max_exec）；真实 `TestRealWriterReplacementReinitialized`（KILL writer 物理连接→重新 Conn()（容忍 ≤5 次 dead）新 ID + lock_wait=5 + NO_ZERO_DATE + 随后 CREATE/INSERT/SELECT/DROP 正常目的端事务证重连后 apply 继续工作）。
- **R6-4（typed ${ENV} 标量：恢复 int/bool/float 兼容，保持结构不可注入，P2）**：round-5 的 AST value expansion 把 `parallel: ${P}`（P=8）替换后 node 仍是 string，严格解码相当于 `parallel: "8"` 拒收——**兼容性回归**。绝不回到 raw text substitution。修：`typedEnvFields`（port/parallel/chunk_size/drill_limit/max_allowed_packet/batch_size/sample_limit=int；tolerance=float；snapshot/drill/no_trim/fold_case/normalize_json/allow_tz_swap/strict_types/secure/allow_unenforced_readonly/no_sync_schema/allow_structure_truncate/allow_row_rewrite=bool）；`expandEnvValue` 递归时把**外层 mapping 的键名**传进值标量（序列项无键）。`expandEnvScalar`：替换后若**整个值恰是一个 ${ENV}**（lone placeholder）+ plain（未加引号——显式引号=刻意 string）+ 字段在 map → 按字段 Go 类型 `strconv` 解析（ParseInt/ParseFloat/ParseBool）成功→设 node tag（`!!int`/`!!float`/`!!bool`）；失败→报错指名变量+值+字段（fail closed）。边界：解析只针对**孤立标量**——`ALLOW="true\nother_key: x"` 对 bool 字段是**报错**（不是新键）；部分替换（值含占位符之外的文字）保持 string（typed 字段上被严格解码拒收=显式失败）；未加类型字段（host/user/password…）保持 string 字节级；加引号占位符保持 string。测试：R5 全部 8 组保留（特殊字符字节级/结构注入/unset fail closed/键永不展开/序列项/不递归/多文档/KnownFields）+ 新增：`parallel: ${P}`=8→int 8 / `snapshot: ${S}`=true / `tolerance: ${T}`=0.001 / `allow_structure_truncate` true·false 双向 / port 3307 / 三类非法值（non-int/`1e`/non-bool）报错指名 / `"true\nother_key: x"` 对 bool 拒收且对 untyped 字段仍是值（字节级）/ untyped 字段数值值保持 string / 序列项保持 string / 加引号占位符 typed 字段拒收·string 字段合法 / 部分替换 typed 拒收·string 拼接。
- **R6-5（文档与实现同步）**：README/MANUAL 删除全部"解析前原文替换/值含引号换行慎用"旧描述（round-5 前的行为，已过时），统一为新语义：${ENV} 在 YAML 结构解析完成后仅替换取值标量；映射键不展开；变量内容无法注入 YAML 结构（新键/新文档/翻安全开关都不可能）；unset fail closed；支持 string/int/bool/float 等合法目标 scalar 类型（typed 字段 lone placeholder 按字段 Go 类型解析，其余保持 string，非法值报错指名）。README/MANUAL 安全性节补逐次重放语义：read-only 策略与会话护栏在**每次 checkout 使用前**重放（KILL/断网/重启替换、带外重置的新物理会话交出前重新拿完整策略，先策略后查询）。
- **R6-6（回归中发现的潜伏正确性 bug：plan 块序=map 序，P0，spec 外追加）**：`foldDigests`（compare）遍历 digest **map** 生成 `DiffChunks` → Go map 迭代随机 → `DiffChunks`/`Plan.Chunks`/executor 的 `w.ops` 组序随机。而**跨块唯一 holder 的"安全"裁决显式假设块按键序顺序 apply**（`holderInOtherChunk`：holder 块在前、其写入先释放唯一槽，写者块在后）——随机序下"释放块"排在"写入块"之后 → 唯一索引 1062，apply 中途失败（小 chunk + 跨块值移动场景必炸）。`TestUniqueHolderParallelOneDoesNotDeadlock`（ChunkSize=1）复现：修前 ~1/6 失败率（`Duplicate entry 'B' for key 't_hold1.v'`，group 0），修后 15/15 全过。修（两层）：① `foldDigests` 差异块**按块 ID 升序**输出（map 先收 ID 再 `sort.Ints`；报告与计划同源确定性——fingerprint 本身不受影响，`TableFingerprint` 内部按 ID 排序）② `DecidePlan` 对 `p.Chunks` 再 `sort.Ints`——**apply 顺序是安全属性**，在计划层钉死（上游任何路径给出乱序块列表，执行器仍按键序）。测试：`TestFoldDigestsDiffChunksSorted`（8 块全异 ×200 次同 map 折叠，逐次断言严格升序——修前代码失败概率≈1）+ `TestDecidePlanChunkList` 加乱序输入 `[7,2,0,5]`→`[0,2,5,7]` 断言。
- **结果**：gofmt/vet 干净；单测全绿（含 `-race`）；主 e2e / 5.7 / TiDB / make push 脚本 / CI 四任务见提交。
- **12 条不变量逐项核对**：① src 零写（applySession 拒绝即不交出，sync 只开 dst writer）② scan/control 使用前策略（逐次 checkout 重放，R6-1/R5-4 双层）③ writer 唯一可写 ④ writer 只连 dst（OpenWriter 只接受 dst ep，调用点唯一）⑤ 作用域不扩大（scopeGate+指纹门不变）⑥ DROP 新鲜重查（R4-2 不变）⑦ rewrite 身份门（R4-3 不变）⑧ 可执行值全参数化（InterpolateParams=false 显式钉死）⑨ YAML 未知字段 fail closed（R4-4 不变）⑩ ENV 不改结构（R5-2+R6-4：typed 只解析孤立标量，结构注入测试保留）⑪ parallel=1 正式支持（R5-1 不变，holder 走 pinned 连接；control 单连接池经 WithControl 闭环释放不会自死锁）⑫ 特殊字符凭据字节级（驱动 formatter/parse round-trip 单测 + 真实账号 e2e 闭环）。
