# mtdiff 使用手册（人话版）

> 赶时间只看前三节；其他部分按你的场景找。技术细节（设计、测试）见 [README.md](README.md)。

## 这东西是干嘛的

你有两个 MySQL（或者 TiDB、PolarDB-X，凡是说 MySQL 协议的都行），想知道"两边的表数据一不一样"。
跑一下它，它告诉你三种结果之一：**一致**、**不一致（哪张表、哪一行）**、**比不了（报错）**。

典型场景：

- 升级 / 迁移前后，核对新旧实例数据是否一致
- 检查主从（源库 / 副本）是否漂移
- 跑完数据订正脚本，确认改对了

它不是把数据全拉出来一行行比（那样又慢又费网），而是**分块算指纹**：两边某一块指纹一样就算一致，只对不一样的块回头细看。千万行的表，内存也就百 MB 量级，不会把库里的数据整个读进内存。

## 安装

```sh
make build
```

得到一个单文件 `bin/mtdiff`，拷到哪都能跑，没有别的依赖。

## 三十秒上手

```sh
# 比较两个库里所有共有的表
./bin/mtdiff \
  --src root:pass@10.0.0.1:3306/dbA \
  --dst root:pass@10.0.0.2:3306/dbB
```

连接串格式：`用户:密码@主机:端口/库名`。端口是 3306 可以省，没密码可以省。
（密码写在命令行上会留在 shell 历史里，介意就用下面的 `--src-password-env`。）

"没密码可以省"有一个例外要分清：直接省略（`user@host`）表示"密码未知，交互式询问"；而 `user:@host`（冒号后面是**空的**密码段）是显式声明"这个服务端就是无密码的"（典型：TiDB 默认 root），不再询问——非交互场景（cron / CI）对无密码服务端必须用 `user:@host`，否则会因为没法弹询问而直接失败。

输出长这样：

```
TABLE                  SRC_ROWS  DST_ROWS  STATUS     DETAIL
t_large                  100000    100000  OK
t_orders                 52341     52340  DIFFERENT  row count differs (52341 vs 52340); 1/4 chunks differ
```

跑的过程中，大表每 ~10% 会在 stderr 打一行进度（比如 `t_orders dst scan 40% (400/1000 chunks)`、sync 写入时 `t_orders row-level 90% (900/1000 chunks, 3999999 rows written)`）；进度只走 stderr，报告 / `--json` 输出干干净净，管道接 `jq` 不受影响。

然后看退出码（写脚本用）：

| 退出码 | 意思 |
|---|---|
| 0 | 全部一致 |
| 1 | 有差异 |
| 2 | 出错（连不上 / 两边类型对不上 / 数据比不了；sync 的结构漂移默认会先对齐结构，见"怎么同步数据"，加 `--no-sync-schema` 才会报这里） |
| 3 | 参数写错了（比如 `--parallel 0`） |

`diff` 子命令可以省略，`mtdiff diff ...` 和 `mtdiff --src ...` 是一回事。
（比出差异后想直接对齐数据，用 `mtdiff sync`，见"怎么同步数据"一节。）

## 连接怎么给

**细粒度 flag**（密码不想上命令行时用）：

```sh
mtdiff \
  --src-host 10.0.0.1 --src-port 3306 --src-user replica \
  --src-password-env SRC_PWD --src-db dbA \
  --dst-host 10.0.0.2 --dst-user replica \
  --dst-password-env DST_PWD --dst-db dbB
```

`--src-password-env SRC_PWD` 的意思是"密码去环境变量 SRC_PWD 里读"，`--dst-...` 同理。

**YAML 配置文件**（参数多不想一屏幕 flag 时）：

```sh
mtdiff --config cfg.yaml
```

```yaml
src:
  host: 10.0.0.1
  user: replica
  password_env: SRC_MYSQL_PWD   # 密码从环境变量读
  database: dbA
dst: { ... }
options:
  tables: [orders, users]
  parallel: 8
  ignore_columns: [updated_at]
```

命令行参数会覆盖 YAML 里的值；YAML 里可以用 `${环境变量名}` 引用环境变量。
注意这是**原文替换**（解析 YAML 之前替换），变量值里含引号、换行、冒号等会破坏 YAML 结构——只建议用于密码这类简单值，结构化值请直接写在文件里。

**密码的优先级**：环境变量（`password_env`）> 写在文件/命令行里的密码 > 交互式询问。
`password_env` 指向一个未设置的变量会直接报错（而不是静默地无密码连接）。
显式空密码（连接串的 `user:@host`）与"写在文件/命令行里的密码"同级：它压制交互式询问，但让位于 `password_env`。
非交互场景（cron、CI）不会弹询问，直接报连接错误。
放心，任何日志、报错、报告里密码都是打码的（`u:***@h:port/db`），不会泄露。

## 最常用的操作

**只比几张表 / 排除几张表**

```sh
mtdiff --src ... --dst ... --tables orders,users
mtdiff --src ... --dst ... --exclude-tables tmp_xxx
```

默认比较两边都有的表；只在一边存在的表会被跳过（`mtdiff tables --src ... --dst ...` 可以列出两边的表）。

**想知道具体哪一行不一样**

```sh
mtdiff --src ... --dst ... --drill
```

会列出示例差异行：键是什么、源端值、目的端值。
`CHANGED` = 值变了，`MISSING_IN_DST` = 目的端缺，`MISSING_IN_SRC` = 源端缺。
默认最多列 10 行，`--drill-limit 50` 可以加。

**某列允许不一样**（比如 `updated_at` 这种）

```sh
mtdiff --src ... --dst ... --ignore-columns updated_at
```

注意两点：目的端如果有**既不在比较范围、又不在 ignore 列表**的列，会直接报错，不会悄悄跳过——宁可错报不放过；ignore 里的列名拼错了等于没写，同样会报出来。

**加个条件过滤**（只比这个月的订单）

```sh
mtdiff --src ... --dst ... --where "create_time >= '2026-08-01'"
```

条件原样作用在两边，行数、切块、比较都按过滤后的来。这是你自己提供的 SQL 片段，自己保证合法。

**表没有主键**

能比，但语义变成"行袋子"（多集合）比较：行顺序无所谓、重复行按次数算，
能告诉你"某个值源端有 3 次、目的端有 2 次"，但**指不出具体哪一行**。
能给出一个可用的列（哪怕不唯一）就能升级成行级定位：

```sh
mtdiff --src ... --dst ... --tables t_nopk --key w
```

自动选键的规则：主键 > 所有列都不允许 NULL 的唯一索引 > 都没有就按无键处理。
唯一索引建在可空列上的**不会**拿来切块（NULL 行切不干净），宁可走无键路径。
显式 `--key` 指到可空列会给你一条告警。

**浮点数两边有精度差**

```sh
mtdiff --src ... --dst ... --tolerance 1e-9
```

默认浮点是**逐位精确**的，差最后一位也算差异；显式给容差才按桶归并。

**数据还在被写（怕比较窗口不一致）**

```sh
mtdiff --src ... --dst ... --snapshot
```

每张表在一个一致性快照事务里扫完，扫描期间的写入不影响结果。
代价是慢，长事务占 read view，千万行级慎用。

**一边 DATETIME、一边 TIMESTAMP**

```sh
mtdiff --src ... --dst ... --allow-tz-swap
```

TIMESTAMP 存的是绝对时刻（两边都强制按 UTC 比，不同时区的机器写同一时刻判相等，这是对的）；
DATETIME 是纯墙钟，两者语义不同，默认直接报错，显式开了才按时刻比。

**字符串大小写 / 尾部空格**

- 默认：裁掉尾部空格（贴近 CHAR 语义）、大小写敏感、逐字节比。
- `--fold-case` 忽略大小写；`--no-trim` 不裁空格。
- JSON 列默认按原始字节比；`--normalize-json` 先做规范化（键排序、数字归一）再比。

**表里有超大 BLOB**

```sh
mtdiff --src ... --dst ... --max-allowed-packet 67108864
```

**接 CI**

```sh
mtdiff --src ... --dst ... --json | jq .ok
# 输出 false 就是有差异；或者干脆只看退出码
```

CI 里密码放环境变量，配合 `--src-password-env` / `--dst-password-env`。

## 它怎么判"一样"（别把结果用错）

- **NULL、空串、0 是三回事**，互不混淆。
- **DECIMAL 按十进制归一化**：`1.00` 和 `1` 算一样；全程字符串处理，绝不经过 float。`decimal(10,2)` 对 `decimal(12,3)` 能比（会提示）。
- **FLOAT/DOUBLE 默认逐位精确**；`--tolerance` 才开容差。
- **BIT 按数值比**：bit(1) 的 1 和 bit(8) 的 1 是同一个值。
- **字符串默认精确**：大小写敏感，只裁尾部空格；两边 collation 不同时提示一声，然后按字节比（宁可误报，不可漏报）。
- **日期**：TIMESTAMP 按时刻（UTC）比，DATETIME 按墙钟比，两者默认互不兼容。
- **零日期（`0000-00-00`）不支持**：遇到会明确报错。用 `--ignore-columns` 把那一列排除掉，或者先修数据。
- **类型兼容**：INT 对 BIGINT 这类"同类数字"允许（会提示）；DATETIME 对 TIMESTAMP 要 `--allow-tz-swap`；JSON 对 TEXT 这种跨类直接报错。想全部按字节级严格来，`--strict-types`。

## 怎么同步数据（sync）

比出差异之后，可以直接把**源库（src）的数据同步到目的库（dst）**，让 dst 变得和 src 一模一样：缺的行补上、值变了的行改掉、dst 多出来的行删掉。方向固定是 src → dst，**只会写 dst，绝不写 src**。

```sh
# 第一步永远是 dry-run：只读对比 + 打印同步计划和示例 SQL，一个字节都不写
mtdiff sync --src ... --dst ... --tables orders,users

# 确认没问题，才真正执行；交互确认后只写 dst
mtdiff sync --src ... --dst ... --tables orders,users --apply

# CI / 脚本里没有终端交互，用 --yes 跳过确认
mtdiff sync --src ... --dst ... --tables orders,users --apply --yes
```

**默认是 dry-run（零写入）**。不加 `--apply` 时，它跑一遍只读对比，告诉你每张表打算怎么同步、给几条示例 SQL（`--sample-limit` 控制条数），然后退出——**不会写任何东西**。`--apply` 才会在（交互）确认后真正写库；非终端（cron / CI）下 `--apply` 必须配 `--yes`，否则报参数错误（退出码 3）而不是傻等输入。

**先同步表结构（默认开启）：**

写数据之前，它会先比对两边的表结构。dst 的结构如果和 src 漂了（缺列、类型变了、多了列、缺主键 / 唯一索引），会先用一条 `ALTER TABLE` 把结构对齐，再对这张表做全量重灌。dry-run 会先把要执行的 DDL 打出来（零写入），`--apply` 确认后才执行 DDL 再写数据。也就是说，结构漂移不再是同步的拦路虎，而是被自动修掉。想保持旧的"结构不一致直接报错"行为，用 `--no-sync-schema`。

**两种同步方式，自动选：**

- **逐行（row-level）**：dst 行数 ≤ src 时，只动有差异的行——缺的 INSERT、变的 UPDATE、多的 DELETE。
- **全量重灌（TRUNCATE + resync）**：当 **dst 行数 > src**、**表没有可用键**（没法定位到具体行）、或**表结构有漂移**时，先把 dst 这张表 `TRUNCATE` 掉，再从 src 整表灌回来。

写完之后会**自动重跑一次对比复验**，退出码以复验为准：真的对齐了才是 0，还有差异就是 1（不会假报成功）。

**几个要清楚的点：**

- 无键表（没有主键 / 唯一键）只要和 src 有差异，就走全量重灌——没法按行定位。
- **结构同步会 `DROP COLUMN`**：dst 上多出来的列（src 没有、也不在 `--ignore-columns` 里的）会被直接删掉——删列不可逆。dry-run 会显示 DDL、确认提示会对涉及结构变更的表点名（`structure+resync`）；不放心就先看看 DDL，或者加 `--no-sync-schema` 只同步数据。
- **表达式默认值不伪造**：src 的列默认值如果是个非字面表达式（MySQL 8.0.13+ 的 `(expr)` 形式，比如 `(uuid())`），结构同步不会替它猜一个值，这张表直接报错。
- **DATETIME 和 TIMESTAMP 互换不算结构变更**：两种类型互换不产生 DDL（换类型会改写存储值的语义），这种表仍按数据比较的规则处理（默认报错，`--allow-tz-swap` 才放行）。
- `--where` 过滤 + 无键表**没法同步**（退出码 3）：TRUNCATE 是整表操作， honoring 不了过滤条件。
- `TRUNCATE` 会**重置 `AUTO_INCREMENT`**，dst 的自增列会从 1 重新开始；需要 `DROP` 权限。
- **写入按批发**：每条多行 INSERT 最多 `--batch-size` 行（默认 1000），小行大表可调到 5000~10000 减少 commit 次数；单条语句受 `--max-allowed-packet` 字节预算限制，单行超预算会如实报错。
- 只写 dst：src 连接、以及两侧所有扫描 / 控制连接在 sync 里也一律强制只读，做不到就拒绝运行（无法强制只读的后端如 TiDB，需 `--allow-unenforced-readonly` 显式豁免，见"敢不敢上生产"）。
- **逐行同步覆盖 src 的键范围，并额外清理范围外的行**：键值落在 src 最小～最大键之外的 dst 行行级操作够不到，sync 会显式扫一遍 dst，把范围外的行**逐键删除**（严格比较：等于边界的行属于范围内、归行级 diff 管，不会被误删；复合键、NULL 值、字符键都处理正确，不做谓词批量盲删）。无 `--where` 时首轮直接收敛，全量重灌只留作计数失配时的安全网；带 `--where` 时只删**匹配过滤条件**的范围外行，不匹配的会保留（过滤表不能 TRUNCATE；复验会如实报 1，无过滤 diff 能看到它们，需人工处理）。唯一的例外是 src 匹配 `--where` 条件为 0 行：此时直接删光 dst 的匹配行，一遍即可对齐。
- **写连接继承读侧护栏**（锁等待 5 秒、单条语句限时 5 分钟，尽力而为）：极慢的大 batch 可能被限时杀掉，该 chunk 回滚、表记失败——调大 `--max-allowed-packet` 或换低峰时段。
- 数据在同步期间又被并发写：应用前会重新计数，计数对不上就升级为全量重灌；最终以复验为准，仍有差异会报 1，不会谎报。

## 敢不敢上生产

- **只读，硬保证（diff / tables）**：每条连接用之前先强制只读会话（MySQL 本体没有会话级 read_only 时回退到只读事务），做不到就拒绝运行，绝不向被比的库发写语句。唯一例外是**无法强制只读的后端**（TiDB：`read_only` 只有 GLOBAL 级，只读事务是禁用的空操作）：默认同样拒绝；确要在这种后端上跑，用 `--allow-unenforced-readonly` 显式豁免，每条读连接会打告警，mtdiff 对它们只发 SELECT。
- **sync 的写入是文档化例外**：默认零写入（dry-run）；只有 `--apply` 且确认后才会写，且**只写 dst**——src 和两侧所有扫描 / 控制连接在 sync 里也一律强制只读。写后自动复验，退出码以复验为准（详见"怎么同步数据"）。
- **影响小**：不持锁；锁等待超时 5 秒、单条语句限时 5 分钟（尽力而为，兼容层不支持就跳过）。
- **准不准**：块一致靠指纹，64 位指纹理论上有 2⁻⁶⁴ 的碰撞概率，不能接受就加 `--secure` 升到 128 位；指纹不一致的块会自动下钻到行，所以"有差异"永远带着具体证据，不是空口。
- **兼容层**：TiDB / PolarDB-X 这类 MySQL 协议实现可直接用。MySQL 5.7 与 TiDB（v7.5）已按跨后端兼容套件实测（`make compat-57` / `make compat-tidb`，见"怎么构建 / 测试"）；跨后端的已知差异（整数显示宽度、各自默认 collation、TiDB 单条 ALTER 限制）已在结构比较里归一，无需人工处理。

## 性能

- 内存：流式读，绝不整表进内存；千万行级表约百 MB。
- `--parallel`（默认 4）：并发的块扫描数，大表可以开 8~16。
- `--chunk-size`（默认 10000 行）：块大小。有主键的表按块比，只有差异块才细看；无键表整表一趟。
- **实测**（docker 双 MySQL 8.0，1000 万行聊天形态表，int 主键；当时宿主机有其他负载，数字偏保守）：两边数据相同时，`diff` 默认并发约 **2 分 9 秒**，`--parallel 16` 约 **48 秒**；sync 行级补 500 万缺失行（含写后全量复验）约 **8 分 11 秒**，写入速度 ~1.4 万行/秒；全量重灌 100 万行约 **1 分 49 秒**（~1.1 万行/秒）。
- **1 亿行外推**（线性）：`diff` / sync 前置检查 ≈ **20 分钟**（默认并发）或 **8 分钟**（parallel 16）；行级 sync ≈ 45 分钟固定开销（前置检查 + 写后复验）+ 增量写入（~1.4 万行/秒：差 100 万行约 70 秒，差 1000 万行约 12 分钟）；全量重灌 ≈ **2.5~3 小时**，建议只用于初始迁移。
- 大表建议：比和 sync 都加 `--parallel 16`；sync 把 `--batch-size` 调到 5000~10000（默认 1000，每条多行 INSERT 行数更多、commit 更少）；dst 有并发写的话挑低峰。

## 常见问题

**Q：退出码 2，咋办？**
看报错信息，就三类：连不上（地址 / 密码 / 库名写错）、两边 schema 对不上（见上面类型兼容规则）、表不存在。
schema 对不上这一类分两种情况：**diff 保持原样报错**（它只读，不动结构）；用 **sync** 的话，结构漂移（缺列 / 类型变 / 多列 / 缺键）默认会被先自动对齐——先 dry-run 看它打算执行哪条 DDL，没问题再 `--apply`。不想让它自动对齐结构，加 `--no-sync-schema`。

**Q：刚改的一行为什么说一致？**
表在并发写入，扫描窗口里两边看到的状态不同。加 `--snapshot`。

**Q：提示没有可比的表？**
默认只比两边都有的表。用 `--tables` 显式指定，或者 `mtdiff tables --src ... --dst ...` 先看看两边各有哪些表。

**Q：同一份 DDL 建的表，跨后端（如 8.0 → TiDB）为什么还会报结构漂移？**
多数"漂移"其实是后端的表面差异，已自动归一、不会误报：整数显示宽度（5.7/TiDB 报 `int(11)`，8.0 报 `int`，等价）；两侧 collation 各自等于自己后端的数据库默认值时不算漂移（8.0 默认 `utf8mb4_0900_ai_ci`、TiDB 默认 `utf8mb4_bin`——两边都没显式声明时视为一致），只有显式声明且不一致的 collation 才产生 DDL。真漂移（缺列 / 类型 / 可空 / 默认值 / 键）仍会出 DDL 对齐；其中 TiDB 不允许一条 ALTER 里对同一列做两次操作（如改完列类型又给它加主键，报 8200），mtdiff 会把 DDL 拆成两条顺序执行，随列删除一起消失的索引也不再显式 DROP。

**Q：能反复跑吗？**
比（diff / tables）能，纯只读。挂 cron 每 5 分钟跑一次只看退出码都行。sync 默认也是零写入，可以反复跑 dry-run 看"还差多少"；但 `--apply` 是真写库，别无脑挂 cron，先 dry-run 确认。

**Q：同步会不会写坏源库？**
不会。同步方向固定 src → dst，**只写 dst**。源库的连接，以及两侧所有扫描 / 控制连接，在 sync 里都被强制成只读会话，做不到就拒绝运行——mtdiff 没有任何代码路径会向 src 发写语句。

**Q：sync 的 TRUNCATE 会把目的库的自增 ID 清零吗？**
会。无键表（任一侧）、dst 行数比 src 多、或一侧有可用键而另一侧没有（如 dst 丢了主键）时走全量重灌，`TRUNCATE` 是 DDL，会把 `AUTO_INCREMENT` 重置为 1。如果 dst 的自增列被别的东西引用（外键、应用缓存），先评估。

**Q：dst 里键值落在 src 键范围之外的行会怎样？**
会被删掉。行级操作只作用于 src 最小～最大键范围内，sync 会显式扫一遍 dst，把范围外的行逐键删掉（严格比较：等于边界的行不碰；复合键、NULL 值都 NULL 安全，字符键按字符串值比较）。无 `--where` 时表在首轮直接对齐，不再因为几行杂行逼一次全量重灌；带 `--where` 时只删**匹配过滤条件**的范围外行，不匹配条件的会保留（过滤表不能 TRUNCATE）——复验会如实报 1，用无过滤的 diff 能看到它们，需要人工处理。

**Q：一侧有键、另一侧没有（如 dst 丢了主键）的表会怎样？**
列完全相同时两张表仍可比：diff 自动回退为 keyless 全表多重集比较（报告带一条 warn 说明回退），数据相同仍报一致、真实漂移照旧报不同。sync 无法按键定位行，走 TRUNCATE 全量重灌（配 `--where` 是参数错误，exit 3）；不想动结构就加 `--no-sync-schema`，用默认的结构同步则会先补回缺失的主键/唯一索引再重灌数据。

**Q：怎么构建 / 测试？**

```sh
make build      # 构建
make test       # 单元测试
make e2e        # 端到端：docker 起两个 MySQL 8.0 容器跑全部场景（127 项断言）
make compat-57  # 跨后端兼容：双 MySQL 5.7（69 项断言）
make compat-tidb # 跨后端兼容：MySQL 8.0 源 + 单节点 TiDB 目标（69 项断言，TiDB 侧自动带 --allow-unenforced-readonly）
```
